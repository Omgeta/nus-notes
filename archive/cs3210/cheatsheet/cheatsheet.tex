\documentclass[12pt, a4paper]{article}

\input{preamble}
\input{preamble-cheatsheet}
\input{letterfonts}

\newcommand{\mytitle}{CS3210 Parallel Computing}
\newcommand{\myauthor}{github/omgeta}
\newcommand{\mydate}{AY 25/26 Sem 2}

\begin{document}
\raggedright
\footnotesize
\begin{multicols*}{3}
\setlength{\premulticols}{1pt}
\setlength{\postmulticols}{1pt}
\setlength{\multicolsep}{1pt}
\setlength{\columnsep}{2pt}

{\normalsize{\textbf{\mytitle}}}\\
{\footnotesize{\mydate\hspace{2pt}\textemdash\hspace{2pt}\myauthor}}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                      Begin                         %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Introduction}
Parallel Computing is the simultaneous use of multiple processing units to solve problems efficiently. 

Program Parallelization Steps:
\begin{enumerate}[\roman*.]
  \item Decomposition: split problem into parallel tasks
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item Granularity: size of task
    \end{itemize}
  \item Scheduling: assign tasks to processes/threads
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item Orchestration: imposed synchronisation and communication of tasks to satisfy dependencies
    \end{itemize}
  \item Mapping: bind processes/threads to hardware processing units (e.g. CPU cores)
\end{enumerate}

\subsection{Processes}
Process is an abstraction for a running program:
\begin{enumerate}[\roman*.]
  \item Process ID (PID): uniquely identifies a process 
  \item Process State: indicates execution status
  \item Process Control Block: stores execution context (registers, resources, private address space)
\end{enumerate}
\begin{enumerate}[$-$]\vspace{-2pt}
  \item Costly: syscall overhead, data structures must be allocated, communication goes through OS
\end{enumerate}\vspace{-1pt}
{\centering
  \incimg[.9]{process}
\par}
Interprocess Communication (IPC):
\begin{enumerate}[\roman*.]
  \item Shared Memory (e.g. locks, semaphores)
  \item Message Passing 
  \item UNIX Pipes and Signals
\end{enumerate}
\colbreak

\subsection{Threads}
Threads are independent execution flows within a process:
\begin{enumerate}[\roman*.]
  \item Shared: Resources, Address Space 
  \item Private: Thread ID, Registers, "Stack" (diff. SP)
\end{enumerate}\vspace{-1pt}
\begin{enumerate}[$+$]
  \item Efficient: cheaper creation and context switching
  \item Resource Sharing: process resources can be shared
  \item Concurrent: multithreading on different cores
\end{enumerate}\vspace{-1pt}
{\centering
  \incimg[0.8]{thread}
\par}
Implementations:
\begin{enumerate}[\roman*.]
  \item User-Threads are managed by a userspace library:
    \begin{enumerate}[$+$]\vspace{2pt}
    \item Fast: context switches have low overhead
  \end{enumerate}
  \begin{enumerate}[$-$]\vspace{6pt}
    \item No Parallelism: OS cannot map threads to different execution units 
    \item Threads blocking will block whole process
  \end{enumerate}
\item Kernel-Threads are implemented in the OS:
  \begin{enumerate}[$+$]\vspace{2pt}
    \item Parallel: threads can run across many CPUs 
  \end{enumerate}
  \begin{enumerate}[$-$]\vspace{6pt}
    \item Slower: thread operations are system calls
  \end{enumerate}
\end{enumerate}

Mapping Models:
\begin{enumerate}[\roman*.]
  \item Many-to-One:
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item All user-level threads are mapped to one process
      \item User library schedules user-threads
    \end{itemize}
  \item One-to-One:
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item Each user-thread mapped to one kernel-thread
      \item No user library scheduler needed
    \end{itemize}
  \item Many-to-Many:
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item User-threads mapped to set of kernel-threads
      \item Library scheduler may move user-threads to different kernel-threads
    \end{itemize}
\end{enumerate}
\vspace{-1em}
\colbreak 
\subsection{Synchronisation}
Synchronisation restricts interleavings of concurrent threads to coordinate access to shared resources.

Critical Section protects resources from concurrent access:
\begin{enumerate}[\roman*.]
  \item Safety: nothing bad happens
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Mutual Exclusion: at most $1$ thread in CS
    \end{itemize}
  \item Liveness: something good happens 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Progress: if no thread is in CS, a waiting thread should be granted access 
      \item Bounded Wait: a waiting thread requesting to enter the CS, will eventually enter
    \end{itemize}
  \item Performance: overhead is small w.r.t work done
\end{enumerate}

Symptoms of Incorrect Synchronization:
\begin{enumerate}[\roman*.]
  \item Deadlock: all threads waiting; iff all conditions met
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Mutual Exclusion: $\geq 1$ resource held exclusively
      \item Hold \& Wait: $\geq 1$ process holding one resource while waiting for another
      \item No Pre-emption: resources must be yielded
      \item Circular Wait: set of processes waiting in circles
    \end{itemize}
  \item Starvation: a thread is unable to make progress 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Livelock: due to deadlock avoidance
    \end{itemize}
  \item Race Condition: bug from program outcome depending on unpredictable access order
  \item Data Race: two threads access a shared resource with no synchronisation, and one is a write 
\end{enumerate}

Mechanisms:
\begin{enumerate}[\roman*.]
  \item Lock: primitive \lstinline|acquire()|, \lstinline|release()|
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Spinlock (busy-wait) vs. Mutex (blocking)
      \item Uses hardware atomics (\lstinline|TSL|) or toggle interrupts
    \end{itemize}
  \item Semaphore: atomic counter $\geq 0$; \lstinline|wait()|, \lstinline|signal()|   
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Binary/Mutex: represents single access 
      \item Counting/General: number of threads in CS $\leq N$
      \item Can be manipulated by different thread 
      \item No relation to data being controlled
    \end{itemize}
  \item Monitor: high-level implicit sync data structure
  \item Messages
\end{enumerate}
\vspace{-1em}
\colbreak
\subsubsection{Producer-Consumer}
Processes share a buffer, where producers add items and consumers remove items when not empty.

Unbounded Buffer (\lstinline|mutex = Sem(1)|, \lstinline|items = Sem(0)|)
\begin{minipage}{0.45\columnwidth}
  \begin{lstlisting}
// Producer
x = produce();
mutex.wait();
  buffer.add(x);
mutex.signal();
items.signal();
  \end{lstlisting}
\end{minipage}
\begin{minipage}{0.5\columnwidth}
  \begin{lstlisting}
// Consumer
items.wait();
mutex.wait();
  x = buffer.get();
mutex.signal();
consume(x);
  \end{lstlisting}
\end{minipage}

Finite Buffer (\lstinline|spaces = Sem(K)|):\\
\begin{minipage}{0.45\columnwidth}
  \begin{lstlisting}
// Producer
x = produce();
spaces.wait();
mutex.wait();
  buffer.add(x);
mutex.signal();
items.signal();
  \end{lstlisting}
\end{minipage}
\begin{minipage}{0.5\columnwidth}
  \begin{lstlisting}
// Consumer
items.wait();
mutex.wait();
  x = buffer.get();
mutex.signal();
spaces.signal();
consume(x);
  \end{lstlisting}
\end{minipage}

\begin{minipage}{0.45\columnwidth}
\subsubsection{Lightswitch}
  \begin{lstlisting}[language=Python,basicstyle=\ttfamily\tiny]
class Lightswitch:
  def __init__(self):
    self.counter = 0 
    self.mutex = Sem(1)

  def lock(self, sem):
    self.mutex.wait()
      self.counter++
      if self.counter == 1:
        sem.wait()
    self.mutex.signal()

  def unlock(self, sem):
    self.mutex.wait()
      self.counter--
      if self.counter == 0:
        sem.signal()
    self.mutex.signal()
  \end{lstlisting}
\end{minipage}
\begin{minipage}{0.5\columnwidth}
\subsubsection{Barrier}
  \begin{lstlisting}
bool last = false;
mutex.wait();
count++;
if (count == N) {
  last = true;
  barrier.signal(); 
}
mutex.signal();

barrier.wait();
if (!last) 
  barrier.signal();
  \end{lstlisting}
\end{minipage}

\colbreak
\subsubsection{Reader-Writer}
Processes share a critical region, where readers can read simultaneously but writers must have exclusive access.
\vspace{-1em}
\begin{minipage}{0.45\columnwidth}
  \begin{lstlisting}
// Writer
empty.wait();
  write();
empty.signal();
  \end{lstlisting}
\end{minipage}
\begin{minipage}{0.5\columnwidth}
  \begin{lstlisting}
// Reader 
mutex.wait();
  readers++;
  if (readers == 1)
    empty.wait();
mutex.signal()
read();
mutex.wait();
  readers--;
  if (readers == 0)
    empty.signal();
mutex.signal()
  \end{lstlisting}
\end{minipage}

Lightswitch:\\
\begin{minipage}{0.45\columnwidth}
  \begin{lstlisting}
// Writer
empty.wait();
  write();
empty.signal();
  \end{lstlisting}
\end{minipage}
\begin{minipage}{0.5\columnwidth}
  \begin{lstlisting}
// Reader 
rswitch.lock(empty);
  read();
rswitch.unlock(empty);
  \end{lstlisting}
\end{minipage}

No Starvation (\lstinline|gate = Sem(1)|):\\
\begin{minipage}{0.45\columnwidth}
  \begin{lstlisting}
// Writer
gate.wait();
empty.wait();
  write();
gate.signal();
empty.signal();
  \end{lstlisting}
\end{minipage}
\begin{minipage}{0.5\columnwidth}
  \begin{lstlisting}
// Reader 
gate.wait();
gate.signal();
rswitch.lock(empty);
  read();
rswitch.unlock(empty);
  \end{lstlisting}
\end{minipage}

Priorities:\\
\begin{minipage}{0.45\columnwidth}
  \begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
// Writer
wswitch.lock(noRead);
noWrite.wait();
  write();
noWrite.signal();
wswitch.unlock(noRead);
  \end{lstlisting}
\end{minipage}
\begin{minipage}{0.5\columnwidth}
  \begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
// Reader 
noRead.wait();
rswitch.lock(noWrite);
noRead.signal();
  read();
rswitch.unlock(noWrite);
  \end{lstlisting}
\end{minipage}


\colbreak
\subsubsection{Dining Philosophers}
$N$ philosophers around a circular table, with a single chopstick between. $2$ chopsticks are needed to eat.
\begin{minipage}{0.48\columnwidth}
  \begin{lstlisting}
// Philosopher
void p(int i) {
  while (1) {
    // think
    take(i);
    eat();
    put(i);
  }
}

void safe_to_eat (int i) {
  if (state[i] == HUNGRY && state[LEFT] != EATING && state[RIGHT] != EATING) {
    state[i] = EATING;
    signal(s[i]);
  }
}
  \end{lstlisting}
\end{minipage}
\begin{minipage}{0.45\columnwidth}
  \begin{lstlisting}
// Take Chopstick
void take(int i) {
  wait(mutex);
  state[i] = HUNGRY;
  safe_to_eat(i);
  signal(mutex);
  wait(s[i]);
}

// Put Chopstick
void put(int i) {
  wait(mutex);
  state[i] = THINKING;
  safe_to_eat(LEFT);
  safe_to_eat(RIGHT)
  signal(mutex);
}
  \end{lstlisting}
\end{minipage}
Limited Eater Solution, when at most $N-1$ philosophers eat concurrently, it's guaranteed at least one can eat:
\begin{lstlisting}
void philosopher(int i) {
  while (1) {
    // think 
    wait(seats);
    wait(chopstick[LEFT]);
    wait(chopstick[RIGHT]);
    eat();
    signal(chopstick[RIGHT]);
    signal(chopstick[LEFT]);
    signal(seats);
  }
}
\end{lstlisting}
\colbreak
\section{Architecture}
Levels of Parallelism:
\begin{enumerate}[\roman*.]
  \item Bit-Level: operate on wider words
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item SIMD: operate on superword/vector registers 
    \end{itemize}
  \item Instruction-Level:
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item Pipeline: split execution into stages; multiple instructions can occupy different stages in same cycle (if no dependency hazards)
      \item Superscalar: duplicate pipelines; issue multiple independent instructions per cycle
    \end{itemize}
  \item Thread-Level: execute multiple threads on the same core in parallel (i.e. SMT)
  \item Processor-Level: execute multiple threads/processes in parallel on multiple cores/processors
\end{enumerate}

Flynn Taxonomy of Parallel Architectures:
\begin{enumerate}[\roman*.]
  \item Single Instruction Single Data (SISD):\\single stream, each instruction on single data
  \item Single Instruction Multiple Data (SIMD):\\single stream, each instruction on multiple data
  \item Multiple Instruction Single Data (MISD):\\multiple streams, each instruction on same data
  \item Multiple Instruction Multiple Data (MIMD):\\per-PU stream to work on its own data
\end{enumerate}

\subsection{Distributed-Memory}
Distributed-Memory Systems are multicomputer systems:
\begin{enumerate}[\roman*.]
  \item Node: indep. unit of processor and private memory 
  \item Interconnection Network: supports message passing for data exchange between nodes
\end{enumerate}
\begin{center}
  \incimg[0.6]{distributedmemory}
\end{center}
\colbreak

\subsection{Shared-Memory}
Shared-Memory Systems are multiprocessor systems:
\begin{enumerate}[\roman*.]
  \item Shared Memory Provider: maintains shared address space abstraction across PUs 
\end{enumerate}
\begin{enumerate}[$+$]\vspace{-2pt }
  \item No need to partition code or data
  \item Efficient communication: no need to physically move data between processors
\end{enumerate}
\begin{enumerate}[$-$]\vspace{-2pt }
  \item Special synchonisation constructs required 
  \item Limited scalability due to contention 
\end{enumerate}

Uniform Memory Access (UMA):
\begin{enumerate}[\roman*.]
  \item Same memory access latency for every PU
\end{enumerate}
\begin{enumerate}[$+$]\vspace{-2pt }
  \item Suitable with few processors due to contention
\end{enumerate}
{\centering
  \incimg[0.7]{uma}
\par}

Non-Uniform Memory Access (NUMA):
\begin{enumerate}[\roman*.]
  \item Different memory access latency for every PU\\(e.g. local memory faster than remote)
  \item Cache Coherent NUMA (ccNUMA):\\each node has coherent cache to reduce contention
\end{enumerate}
{\centering
  \incimg[0.7]{numa}
\par}

Cache Only Memory Access (COMA):
\begin{enumerate}[\roman*.]
  \item Memory blocks behave as caches 
  \item Data migrates dynamically and continuously according to cache coherence scheme
\end{enumerate}
{\centering
  \incimg[0.7]{coma}
\par}
\colbreak
\subsection{Cache Coherence}
Caches reduce memory access latency and provide high bandwidth data transfer to CPU: 
\begin{enumerate}[\roman*.]
  \item Cache Size: larger cache reduces misses but increases access time (addressing complexity) 
  \item Block Size: unit of transfer; larger blocks improve spatial locality, but longer transfer time
\end{enumerate}

Write Policy:
\begin{enumerate}[\roman*.]
  \item Write-through: write to both cache and memory
    \begin{enumerate}[$+$]\vspace{3pt}
      \item Always get newest value of memory block 
    \end{enumerate}
    \begin{enumerate}[$-$]\vspace{6pt}
      \item Slower due to many memory accesses; mitigate with write buffer
    \end{enumerate}
  \item Write-back: write to cache only and set dirty bit; write to memory on cache block eviction
    \begin{enumerate}[$+$]\vspace{3pt}
      \item Fewer write operations to memory
    \end{enumerate}
    \begin{enumerate}[$-$]\vspace{6pt}
      \item Memory may contain invalid entries 
    \end{enumerate}
\end{enumerate}

Coherence defines behaviour of reads/writes to the same memory location. Properties:
\begin{enumerate}[\roman*.]
  \item Program Order: if $P$ writes to $X$, n.f.w to $X$,\\$P$ should read same value from $X$ 
  \item Write Propagation: if $P_1$ writes to $X$, n.f.w to $X$, $P_2$ should read same value from $X$ eventually
  \item Transaction Serialization: if $V_1 \rightarrow X$, then $V_2 \rightarrow X$,\\all $P_i$ should never read $V_2$ then $V_1$ from $X$
\end{enumerate}

Implementations: 
\begin{enumerate}[\roman*.]
  \item Software-based: use page-fault to propagate writes
  \item Snooping-based: cache controllers snoop/broadcast on shared bus
  \item Directory-based: central directory records sharing status; no broadcast; common in NUMA
\end{enumerate}

Implications:
\begin{enumerate}[\roman*.]
  \item "Increase" memory latency and lower cache hit rate
  \item Cache Ping-Pong: 2 PUs write to same cache line; invalidations slow fetch from memory
  \item False Sharing: PUs write to different addresses on same cache line; avoid with padding
\end{enumerate}
\vspace{-1em}
\colbreak
\subsection{Memory Consistency}
Memory Consistency constrains the order in which memory operations by a thread become visible (commit) to other threads for different memory locations. 

Memory Ordering Requirements:
\begin{enumerate}[\roman*.]
  \item $W \rightarrow R$: write to $X$ commits before read from $Y$
  \item $R \rightarrow R$: read from $X$ commits before read from $Y$
  \item $R \rightarrow W$: read from $X$ commits before write to $Y$
  \item $W \rightarrow W$: write to $X$ commits before write to $Y$
\end{enumerate}

Sequential Consistency Model (SC):
\begin{enumerate}[\roman*.]
  \item Preserves all four ordering requirements
  \item PUs issue memory operations in program order
  \item Interleaving of PUs' program orders is acceptable
\end{enumerate}
\begin{enumerate}[$-$]\vspace{-2pt }
  \item Can result in loss of performance
\end{enumerate}

Relaxed Consistency Model:
\begin{enumerate}[\roman*.]
  \item Relax ordering only if no data dependencies between operations to same memory location:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item $R\rightarrow W$: anti-dependence (WAR)
      \item $W\rightarrow W$: output dependence (WAW)
      \item $W\rightarrow R$: flow dependence (RAW)
    \end{itemize}
  \item Override constructs can prevent certain re-orderings
  \item Total Store Ordering (TSO):
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Relax $W\rightarrow R$: a PU may read $B$ before its earlier write to $A$ is visible to \emph{all} other PUs
      \item Write Atomicity: writes become visible to all PUs at the same time 
    \end{itemize}
  \item Processor Consistency (PC):
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Relax $W\rightarrow R$
      \item No Write Atomicity: a write may be seen by some PUs before others
    \end{itemize}
  \item Partial Store Ordering (PSO): 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Relax $W\rightarrow R$
      \item Write Atomicity
      \item Relax $W\rightarrow W$: a later write to $B$ can bypass an earlier write to $A$
    \end{itemize}
\end{enumerate}
\begin{enumerate}[$+$]\vspace{-2pt }
  \item Hide latencies by overlapping indep. operations
\end{enumerate}
\vspace{-1em}
\colbreak
\subsection{Interconnection Networks}
Interconnection forms the backbone of communication between processors, memories/caches, and I/O devices.

\subsection{Topology}
Topology is the graph structure of the network.

Direct Interconnect: endpoints connected directly, usually of same type (e.g. core--core) 
\begin{enumerate}[\roman*.]
  \item Diameter, $\delta(G)$: maximum shortest-path distance between any pair of nodes
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Small diameter $\Rightarrow$ shorter message transmission
    \end{itemize}
  \item Degree, $g(v)$: no. of nodes adjacent to node $v$; $g(G)$ is the maximum node degree in network $G$
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Small degree $\Rightarrow$ lower node hardware overhead.
    \end{itemize}
  \item Bisection Width, $B(G)$: minimum no. of edges removed to split the network into two equal halves
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Bisection bandwidth $BW(G)$: total bandwidth between the two bisections 
      \item Measures capacity in the worst case 
    \end{itemize}
  \item Node Connectivity, $nc(G)$: minimum no. of nodes whose failure disconnects the network
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Determines robustness of the network
    \end{itemize}
  \item Edge Connectivity, $ec(G)$: minimum no. of edges whose failure disconnects the network
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Determines number of independent paths between any pair of nodes
    \end{itemize}
\end{enumerate}

\renewcommand{\arraystretch}{1.05}
\setlength{\tabcolsep}{3pt}
\begin{tabular}{|c|c|c|c|c|c|}
\hline
& $n$ & $g(G)$ & $\delta(G)$ & $ec(G)$ & $B(G)$ \\\hline
Complete Graph & $n$ & $n-1$ & $1$ & $n-1$ & $(\frac{n}{2})^2$\\\hline 
Linear Array & $n$ & $2$ & $n-1$ & $1$ & $1$\\\hline
Ring & $n$ & $2$ & $\lfloor \frac{n}{2}\rfloor$ & $2$ & $2$\\\hline
$d$-dim Mesh & $r^d$ & $2d$ & $d(\sqrt[d]{n} - 1)$ & $d$ & $n^{\frac{d-1}{d}}$\\\hline
$d$-dim Torus & $r^d$ & $2d$ & $d\lfloor \frac{\sqrt[d]{n}}{2}\rfloor$ & $2d$ & $2n^{\frac{d-1}{d}}$\\\hline
$k$-dim Hypercube & $2^k$ & $\log n$ & $\log n$ & $\log n$ & $\frac{n}{2}$\\\hline
$k$-dim CCC & $k2^k$ & $3$ & $2k-1 + \lfloor \frac{k}{2}\rfloor$ & $3$ & $\frac{n}{2k}$\\\hline
Complete BT & $2^k-1$ & $3$ & $2\log \frac{n+1}{2}$ & $1$ & $1$\\\hline
$k$-ary $d$-cube & $k^d$ & $2d$ & $d\lfloor \frac{k}{2}\rfloor$ & $2d$ & $2k^{d-1}$\\\hline
\end{tabular}

\colbreak
Indirect Interconnection: endpoints communicate through dynamically configured switches:
\begin{enumerate}[$+$]
  \item Reduce hardware costs by sharing switches \& links
  \item Sometimes connect different types of hardware\\(e.g. PUs and memory modules)
\end{enumerate}\vspace{-3pt}
\begin{enumerate}[\roman*.]
  \item Cost: number of switches and links 
  \item Concurrent connections
\end{enumerate}

Types:
\begin{enumerate}[\roman*.]
  \item Bus: set of wires carry data from sender to receiver
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Only a pair of devices can communicate at a time 
      \item Bus arbiter used for coordination 
      \item Typically used for small number of processors
    \end{itemize}
  \item Crossbar: switch matrix (of $n\times m$ switches) from $n$ inputs to $m$ outputs
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Switch states: straight or direction change 
      \item Hardware costly $\rightarrow$ small number of processors
    \end{itemize}
  \item Multistage Switching: intermediate switches with connecting wires between neighbouring stages
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Goal: obtain small distance for any pair of input and output devices
    \end{itemize}
  \item Omega: unique path from each input to each output ($n\times n$)
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Construction: switch $(\text{pos }\alpha, \text{stage }i) \rightarrow (\beta,i+1), (\beta', i+1)$ \\where $\beta$ is cyclic-left-shift of $\alpha$,\\$\beta'$ is cyclic-left-shift + inversion of LSB
      \item $\log n$ stages of $\frac{n}{2}$ switches per stage
      \item Connections between stages are regular\\$\rightarrow$ good for manufacturing
    \end{itemize}
  \item Butterfly:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Construction: switch $(\alpha, i) \rightarrow (\alpha,i+1), (\alpha', i+1)$ where $\alpha, \alpha'$ differ in the $(i+1)^{th}$ bit from the left
    \end{itemize}
  \item Baseline:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Construction: switch $(\alpha, i) \rightarrow (\beta,i+1), (\beta',i+1)$\\where $\beta$ is cyclic-right-shift of last ($k-i$) bits,\\$\beta'$ is inversion of LSB + same cyclic-right-shift
    \end{itemize}
\end{enumerate}

\colbreak

\subsection{Routing}
Routing determines path(s) from source to destination within a given topology.
Algorithm Classification:
\begin{enumerate}[\roman*.]
  \item Path Length: 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Minimal: shortest-path always chosen 
      \item Non-minimal: shortest-path not necessary
    \end{itemize}
  \item Adaptivity: 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Deterministic: always use same path for same pair of source and destination nodes 
      \item Adaptive: take into account network status\\(e.g. avoid congested path, avoid dead nodes)
    \end{itemize}
\end{enumerate}

Deterministic Algorithms:
\begin{enumerate}[\roman*.]
  \item XY Routing for 2D Mesh: 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Move in $X$ direction until $X_{src} == X_{dst}$
      \item Move in $Y$ direction until $Y_{src} == Y_{dst}$
    \end{itemize}
  \item E-Cube Routing for Hypercube:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Bit difference in source and target node address (hamming distance) $=$ number of hops
      \item From MSB$\rightarrow$LSB (or vice versa), find first different bit and go to neighbour node with bit corrected; at most dim-$k$ hops
    \end{itemize}
  \item XOR-Tag Routing for Omega Network:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Let $T = $ Source ID $\oplus$ Destination ID
      \item At stage $k$, go straight if bit $k = 0$, else crossover
    \end{itemize}
\end{enumerate}

\subsection{Data Distribution}

Patterns:
\begin{enumerate}[\roman*.]
  \item Blockwise: contiguous chunks per processor;\\ good for spatial locality.
  \item Cyclic: round-robin elements per processor;\\ good for load balance.
  \item Block-cyclic: cyclic distribution of blocks of size $b$; balances locality and load.
\end{enumerate}

Choose distribution based on which processors need which data.

\colbreak
\section{Programming Models}

Types of Parallelism:
\begin{enumerate}[\roman*.]
  \item Data Parallelism: split data among PUs\\(loop parallelism; SIMD/SPMD)
  \item Task Parallelism: split indep. tasks among PUs (statements, loops, function calls)
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Task Dependency Graph: DAG representing control dependency between tasks
        \begin{itemize}[leftmargin=*]\vspace{3pt}
          \item Critical Path Length: slowest completion
          \item Deg. Concurrency: $\displaystyle \frac{\text{Total Work}}{\text{Critical Path Length}}$
        \end{itemize}
    \end{itemize}
\end{enumerate}

Representation of Parallelism:
\begin{enumerate}[\roman*.]
  \item Implicit Parallelism: Haskell
  \item Implicit Scheduling: OpenMP
  \item Explicit Communication \& Sync: MPI, Pthreads
\end{enumerate}

Models of Coordination:
\begin{enumerate}[\roman*.]
  \item Shared Address Space: very little structure
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item All threads can access all shared variables;\\mutual exclusion via locks
      \item Match shared-memory systems (UMA/NUMA); requires HW support; costly to scale 
    \end{itemize}
    \begin{enumerate}[leftmargin=*, label=$+$]\vspace{5pt}
      \item Simple to program, if synced correctly 
    \end{enumerate}
    \begin{enumerate}[leftmargin=*, label=$-$]\vspace{5pt}
      \item Scalability limited by memory cost variation (even with NUMA) or bandwidth limiting speed
    \end{enumerate}
  \item Message Passing: highly structured
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item Tasks operate in private address spaces;\\communication via explicit send/receive 
      \item Matches distributed memory systems
    \end{itemize}
    \begin{enumerate}[leftmargin=*, label=$+$]\vspace{5pt}
      \item Allows scaling across many machines 
    \end{enumerate}
    \begin{enumerate}[leftmargin=*, label=$-$]\vspace{5pt}
      \item Complex programming  
      \item Network communication delays 
    \end{enumerate}
\end{enumerate}

\colbreak

\subsection{Foster's Design Methodology}
\begin{enumerate}[\roman*.]
  \item Partitioning: problem into pieces/data 
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item $\geq 10\times$ primitive tasks than cores, of similar size
      \item Minimize redundant computations and data
      \item Number of tasks increases with problem size
    \end{itemize}
  \item Communication: pass needed data among tasks 
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item Balance communication among tasks
      \item Perform communication in parallel 
      \item Overlap computation with communication
      \item Communicate with small number of neighbours
    \end{itemize}
  \item Agglomeration: combine tasks into larger tasks 
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item Number of tasks $\geq$ number of cores
      \item Reduce cost of task creation + communication
      \item Maintain scalability, simplify code
    \end{itemize}
  \item Mapping: tasks to execution units
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item Maximize processor utilization; minimize IPC
      \item Optimal mapping is NP-hard $\Rightarrow$ heuristics 
      \item Consider static vs dynamic allocation;\\if static, tasks:cores $\ge 10{:}1$;\\if dynamic, allocator must not bottleneck
    \end{itemize}
\end{enumerate}

\subsection{Parallel Programming Patterns}
Patterns:
\begin{enumerate}[\roman*.]
  \item Fork--Join: \lstinline|fork| child tasks, then \lstinline|join| (D\&C)
  \item Parbegin--Parend: mark statements to execute in parallel threads, then sync at end
  \item SIMD: single instruction stream over multiple data elements (synchronous)
  \item SPMD: same program on multiple threads; different data via IDs (asynchronous)
  \item Master--Worker: master distributes tasks to workers
  \item Task Pools: shared pool of tasks; fixed number of worker threads dynamically dequeue/insert tasks
  \item Producer--Consumer: produce tasks into buffer for consumers to retrieve; sync coordinates passing 
  \item Pipelining: split compute into stages for data stream; throughput improves via stage-overlap
\end{enumerate}
\vspace{-1em}
\colbreak
\subsection{OpenMP}
Shared-Memory Programming Model:
\begin{enumerate}[\roman*.]
  \item Threads communicate via shared variables; may also have private variables
  \item Avoid race conditions using mutual exclusion
\end{enumerate} 

\subsubsection{Constructs}
General:
\begin{enumerate}[\roman*.]
  \item \lstinline|#pragma omp parallel|: define parallel region (fork--join), creating team of threads in the block
  \item \lstinline|omp_get_thread_num()|/\lstinline|omp_get_num_threads()|: query a thread's id and the current team size.
  \item \lstinline|omp_set_num_threads(n)| / \lstinline|OMP_NUM_THREADS|: control the number of threads 
\end{enumerate}

Data Parallel For:
\begin{enumerate}[\roman*.]
  \item \lstinline|#pragma omp for|: work-sharing for loops
  \item \lstinline|schedule(kind,size)|:  static vs. dynamic allocation and chunking 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Default: static, blockwise
    \end{itemize}
  \item \lstinline|shared(...)|/\lstinline|private(...)|: data-scoping clauses 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Default: variables declared outside shared;\\variables declared inside private
    \end{itemize}
  \item \lstinline|reduction(op:var)|: private per-thread value, combined at end.
  \item \lstinline|collapse(n)|: flatten \lstinline|n| nested loops for iterations 
\end{enumerate}

Controlled:
\begin{enumerate}[\roman*.]
  \item \lstinline|#pragma omp sections|/\lstinline|#pragma omp section|: split independent blocks among threads
  \item \lstinline|#pragma omp single|: one thread executes block
  \item \lstinline|#pragma omp master|: only master executes block 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Default: no implicit barrier
    \end{itemize}
\end{enumerate}

Synchronisation:
\begin{enumerate}[\roman*.]
  \item \lstinline|#pragma omp barrier|: explicit barrier for threads
  \item \lstinline|#pragma omp critical|: critical section block 
  \item \lstinline|#pragma omp atomic|: atomic update to a location 
  \item \lstinline|nowait|: disable implicit barrier after blocks
\end{enumerate}

\vspace{-1em}
\colbreak
\subsubsection{Parallel Region}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
omp_set_num_threads(4);
#pragma omp parallel
{
  int tid = omp_get_thread_num();
  int nt  = omp_get_num_threads();
  printf("Thread %d out of %d\n", tid, nt);
}
\end{lstlisting}

\subsubsection{Parallel For}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
// a, b, c, N are shared by all threads.
// i and tmp are private to each thread.
// dynamic scheduling assigns chunks of 8 iterations at a time.
#pragma omp parallel for \ 
  shared(a, b, c, N) \ 
  private(i, tmp) \ 
  schedule(dynamic, 8)
for (int i = 0; i < N; i++) {
  int tmp = a[i] + b[i];
  c[i] = tmp;
}
\end{lstlisting}

\subsubsection{Reduction}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
int sum = 0;
#pragma omp parallel for \ 
  reduction(+:sum)
for (int i = 0; i < N; i++) {
  sum += a[i];
}
\end{lstlisting}

\subsubsection{Collapse}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
#pragma omp parallel for \ 
  collapse(2)
for (int i = 0; i < num_rows; i++) {
  for (int j = 0; j < num_cols; j++) {
    C[i][j] = A[i][j] + B[i][j];
  }
}
\end{lstlisting}

\subsubsection{Mutex}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
omp_lock_t lock;
omp_init_lock(&lock);
#pragma omp parallel
{
  omp_set_lock(&lock);
  x++;
  omp_unset_lock(&lock);
}
\end{lstlisting}

\colbreak

\subsubsection{Sections}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
#pragma omp parallel sections
{
  #pragma omp section
  {
    read_input();
  }

  #pragma omp section
  {
    initialize_table();
  }
}
\end{lstlisting}

\subsubsection{Single and Master}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
#pragma omp parallel
{
  #pragma omp single
  {
    init_shared_data();
  }

  #pragma omp master
  {
    printf("Only thread 0 runs this block.\n");
  }
}
\end{lstlisting}

\subsubsection{Critical \& Atomic}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
int x = 0;
int y = 0;
#pragma omp parallel
{
  #pragma omp atomic
  x++;

  #pragma omp critical
  {
    y++;
    printf("y = %d\n", y);
  }
}
\end{lstlisting}

\colbreak
\subsection{NVIDIA CUDA}
CUDA Programming Model:
\begin{enumerate}[\roman*.]
  \item Heterogeneous: Host (CPU) + Device (GPU)
  \item Kernel: device function launched by host 
  \item Thread: smallest execution unit, with private registers/local variables 
  \item Block: group of threads that can cooperate via shared memory and \lstinline|__syncthreads()|
  \item Grid: all blocks launched by a kernel;\\no synchronization across blocks
\end{enumerate}
\vspace{-1em}
\begin{center}
  Function Qualifiers\\
  \begin{tabular}{|c|c|c|}
  \hline
  \textbf{Qualifier} & \textbf{Scope} & \textbf{Caller} \\
  \hline
  \lstinline|__host__|   & CPU & CPU \\
  \hline
  \lstinline|__device__| & GPU & GPU \\
  \hline
  \lstinline|__global__| & GPU & CPU \\
  \hline
  \end{tabular}
\end{center}

CUDA Execution Model:
\begin{enumerate}[\roman*.]
  \item Streaming Multiprocessor (SM): schedules resident thread blocks which do not migrate
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Streaming Processor (SP): executes warps
      \item Multiple blocks may reside on one SM 
    \end{itemize}
  \item Warp: group of 32 threads from same block
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item SIMT: threads in a warp execute same instruction in lockstep when possible
      \item Divergence: branches within a warp reduce throughput, due to stalls for SIMT
      \item Scheduling: ready warps context switched to hide latency if resources allow; registers for assigned warps are already in the register file 
    \end{itemize}
\end{enumerate}
\incimg{gpu}
\vspace{-1em}
\colbreak
CUDA Memory Model:
\begin{enumerate}[\roman*.]
  \item Registers: thread-private, fast and limited 
  \item Local Memory: thread-private, slow, cached 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Used for large data/ arrays
    \end{itemize}
  \item Shared Memory: block-private, fast
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Programmer-managed
      \item Requires \lstinline|__syncthreads()| for synchronization
    \end{itemize}
  \item Global Memory: global, read-write, slow, cached 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Usage: \lstinline|cudaMalloc|, \lstinline|cudaMemcpy|, \lstinline|cudaFree|
    \end{itemize}
  \item Constant Memory: global, read-only, slow, cached
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Better for linear access 
    \end{itemize}
  \item Texture Memory: global, read-only, slow, cached
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Better for 2D spatial access 
    \end{itemize}
\end{enumerate}
\vspace{-1em}
\begin{center}
  \incimg[0.8]{cudamemory}
\end{center}

\begin{center}
Variable Qualifiers
\begin{tabular}{|c|c|c|c|}
\hline
\textbf{Qualifier} & \textbf{Memory} & \textbf{Scope} & \textbf{Lifetime} \\
\hline
auto non-array & register  & thread & kernel \\
\hline
auto array & local  & thread & kernel \\
\hline
\lstinline|__device__ __constant__| & constant & grid  & static \\
\hline
\lstinline|__device__|   & global & grid  & static \\
\hline
\lstinline|__device__ __shared__|   & shared  & block & kernel \\
\hline
\lstinline|__managed__|  & global  & global & static \\
\hline
\end{tabular}
\end{center}
\vspace{-1em}
\colbreak

Coalesced Global Memory Access:
\begin{enumerate}[\roman*.]
  \item Global memory requests are served in fixed-size blocks of 32 bytes (e.g. \lstinline|float| = 4 bytes)
  \item Coalesced: adjacent warp threads access adjacent addresses $\rightarrow$ combined into fewer transactions
  \item Memory Utilization: $\frac{\text{Useful Bytes Requested}}{\text{Total Bytes Requested}}$
\end{enumerate}

Shared Memory Bank Conflicts:
\begin{enumerate}[\roman*.]
  \item Shared memory cyclic divided into 32 banks that can be accessed in parallel
  \item No Conflict: warp threads access different banks $\rightarrow$ parallel access
  \item Bank Conflict: multiple warp threads access different addresses in same bank $\rightarrow$ serialized access
  \item Broadcast: multiple warp threads access same address $\rightarrow$ served efficiently
\end{enumerate}

Optimizations:
\begin{enumerate}[\roman*.]
  \item Minimize host--device data transfers 
  \item Overlap compute: \lstinline|cudaMemcpyAsync| + streams
  \item Minimize global memory access w/ shared memory
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Coalesce global memory accesses
      \item Minimize shared memory bank conflicts 
    \end{itemize}
  \item Balance Occupancy and Utilization: many warps hide latency; many blocks in parallel; but too many warps and data may not fit into registers
  \item Minimize warp divergence 
  \item Prefer high-throughput ops: single precision faster, replace div/mod with bitwise ops
\end{enumerate}

\colbreak
\subsubsection{Kernel Launch and Indexing}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
__global__
void addVec(float *a, float *b, float *c, int N) {
  int i = blockIdx.x * blockDim.x + threadIdx.x;

  if (i < N)
    c[i] = a[i] + b[i];
}

int threads = 256;
int blocks = (N + threads - 1) / threads;

addVec<<<blocks, threads>>>(a_d, b_d, c_d, N);
cudaDeviceSynchronize();
\end{lstlisting}

\subsubsection{Shared Memory}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
__global__
void copyTile(float *in, float *out) {
  __shared__ float tile[256];

  int i = blockIdx.x * blockDim.x + threadIdx.x;
  int t = threadIdx.x;

  tile[t] = in[i];
  __syncthreads();

  out[i] = tile[t];
}
\end{lstlisting}

\subsubsection{Host--Device Memory Transfer}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
float *a_d, *b_d, *c_d;

cudaMalloc(&a_d, N * sizeof(float));
cudaMalloc(&b_d, N * sizeof(float));
cudaMalloc(&c_d, N * sizeof(float));

cudaMemcpy(a_d, a_h, N * sizeof(float), cudaMemcpyHostToDevice);
cudaMemcpy(b_d, b_h, N * sizeof(float), cudaMemcpyHostToDevice);

addVec<<<blocks, threads>>>(a_d, b_d, c_d, N);
cudaDeviceSynchronize();

cudaMemcpy(c_h, c_d, N * sizeof(float), cudaMemcpyDeviceToHost);

cudaFree(a_d);
cudaFree(b_d);
cudaFree(c_d);
\end{lstlisting}

\colbreak
\subsubsection{2D Grid and Block Indexing}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
__global__
void addMatrix(float *A, float *B, float *C, int N) {
  int row = blockIdx.y * blockDim.y + threadIdx.y;
  int col = blockIdx.x * blockDim.x + threadIdx.x;
  int idx = row * N + col;

  if (row < N && col < N)
    C[idx] = A[idx] + B[idx];
}

dim3 threads(16, 16);
dim3 blocks((N + threads.x - 1) / threads.x,
            (N + threads.y - 1) / threads.y);

addMatrix<<<blocks, threads>>>(A_d, B_d, C_d, N);
\end{lstlisting}

\subsubsection{Shared Memory Padding}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
__global__
void transposePadded(float *A, float *B, int N) {
  __shared__ float tile[16][18];

  int row_r = blockIdx.y * blockDim.y + threadIdx.y;
  int col_r = blockIdx.x * blockDim.x + threadIdx.x;

  int row_w = blockIdx.x * blockDim.x + threadIdx.y;
  int col_w = blockIdx.y * blockDim.y + threadIdx.x;

  if (row_r < N && col_r < N)
    tile[threadIdx.y][threadIdx.x] = A[row_r * N + col_r];

  __syncthreads();

  if (row_w < N && col_w < N)
    B[row_w * N + col_w] = tile[threadIdx.x][threadIdx.y];
}
\end{lstlisting}

\colbreak
\subsubsection{Atomic Add}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
__global__
void countPositive(float *a, int *count, int N) {
  int i = blockIdx.x * blockDim.x + threadIdx.x;

  if (i < N && a[i] > 0)
    atomicAdd(count, 1);
}
\end{lstlisting}

\subsubsection{Reduction Sum}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
__global__
void reduceSumAtomic(float *in, float *out, int N) {
  __shared__ float sdata[256];

  int tid = threadIdx.x;
  int i = blockIdx.x * blockDim.x + threadIdx.x;

  sdata[tid] = (i < N) ? in[i] : 0.0f;
  __syncthreads();

  for (int stride = blockDim.x / 2; stride > 0; stride /= 2) {
    if (tid < stride)
      sdata[tid] += sdata[tid + stride];

    __syncthreads();
  }

  // cudaMemset(out, 0, sizeof(float)) in host
  if (tid == 0)
    atomicAdd(out, sdata[0]);
}
\end{lstlisting}
\subsubsection{CUDA Streams}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
cudaStream_t s0, s1;
cudaStreamCreate(&s0);
cudaStreamCreate(&s1);

cudaMemcpyAsync(a0_d, a0_h, bytes,
                cudaMemcpyHostToDevice, s0);
kernel<<<blocks, threads, 0, s0>>>(a0_d);

cudaMemcpyAsync(a1_d, a1_h, bytes,
                cudaMemcpyHostToDevice, s1);
kernel<<<blocks, threads, 0, s1>>>(a1_d);

cudaDeviceSynchronize();

cudaStreamDestroy(s0);
cudaStreamDestroy(s1);
\end{lstlisting}

\colbreak

\subsection{OpenMPI}
Message Passing Model:
\begin{enumerate}[\roman*.]
  \item Distributed Memory: indep. address spaces
  \item Explicit communication with send/recieve
  \item No data races on process-local variables 
  \item Communication races/deadlocks still possible
\end{enumerate}

MPI Program Overview:
\begin{enumerate}[\roman*.]
  \item Initialize only once before any other calls 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item \lstinline|MPI_Init(int *argc, char **argv)|
    \end{itemize}
  \item Communicate to coordinate computation
  \item Exit from message-passing system
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item \lstinline|MPI_Finalize(void)| 
      \item \lstinline|MPI_Abort(MPI_Comm comm, int errorcode)|
    \end{itemize}
\end{enumerate}

\subsubsection{Communication Semantics}
Buffering: whether send data can be copied to an internal system buffer to avoid idling while waiting for receive
\begin{enumerate}[\roman*.]
  \item Buffered: less idling; buffer overhead
  \item Non-buffered: idling + deadlock risks if mismatch
\end{enumerate}

Blocking: whether resources/buffers are safe to reuse on function call return
\begin{enumerate}[\roman*.]
  \item Blocking: return $\Rightarrow$ safe to reuse resources/buffers
  \item Non-Blocking: may return before completion
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item \lstinline|MPI_Test(MPI_Request *r, int *flag, MPI_Status *s)|; \lstinline|flag| marks completion
      \item \lstinline|MPI_Wait(MPI_Request *r, MPI_Status *s)|
    \end{itemize}
\end{enumerate}

Synchronous: whether sends require a receive to start participating before returning
\begin{enumerate}[\roman*.]
  \item Synchronous: operation does not complete before both sides have started
  \item Asynchronous: sender can proceed without coordination with receiver
\end{enumerate}

\colbreak

\subsubsection{Point-to-Point Communication}

Messaging Calls:
\begin{enumerate}[\roman*.]
  \item \lstinline|MPI_Send(void* buf, int count, MPI_Datatype dt, int dst, int tag, MPI_Comm c)| 
  \item \lstinline|MPI_Recv(void* buf, int count, MPI_Datatype dt, int src, int tag, MPI_Comm c, MPI_Status *status)|
\end{enumerate}

Rank Calls:
\begin{enumerate}[\roman*.]
  \item \lstinline|MPI_Comm_size(MPI_Comm comm, int *size)|:\\ no. of processes in communicator
  \item \lstinline|MPI_Comm_rank(MPI_Comm comm, int *rank)|:\\ current process rank
\end{enumerate}

Messages Format:
\begin{enumerate}[\roman*.]
  \item Data: start-buffer + count + datatype
  \item Envelope: dst/src (rank) + tag + communicator
  \item Receive buffer must be \(\ge\) message length
  \item Wildcards: \lstinline|MPI_ANY_SOURCE|, \lstinline|MPI_ANY_TAG|
  \item \lstinline|MPI_Status|: \lstinline|MPI_SOURCE|, \lstinline|MPI_TAG|, \lstinline|MPI_ERROR|
\end{enumerate}

Ordering:
\begin{enumerate}[\roman*.]
  \item Same sender--receiver: sends are non-overtaking
  \item Different tags: may be received out of send order if receives request specific tags
  \item $>2$ processors: undefined order
\end{enumerate}

\subsubsection{Deadlocks}
Deadlock Types: 
\begin{enumerate}[\roman*.]
  \item Message Order: both sides wait on receives
  \item Buffer: no/small buffers $\Rightarrow$ sends cannot complete
\end{enumerate}

Secure MPI program: correctness does not depend on assumptions about MPI runtime properties.
\begin{itemize}
  \item Specify send/receive order\\(e.g. even: send$\rightarrow$recv, odd: recv$\rightarrow$send) 
\end{itemize}

\colbreak
\subsubsection{Collective Communication}

Process Groups are ordered sets of processes:
\begin{enumerate}[\roman*.]
  \item Unique rank identifier, starting from 0 
  \item Processes may be in multiple groups
  \item Communicator: communication domain for a group
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item Intra-communicator: collectives in a group; default \lstinline|MPI_COMM_WORLD|
      \item Inter-communicator: P2P between two groups
    \end{itemize}
  \item \lstinline|MPI_Comm_create|, \lstinline|MPI_Comm_comp|, \lstinline|MPI_Comm_dupl|, \lstinline|MPI_Comm_split|, \lstinline|MPI_Comm_free|
\end{enumerate}
\begin{enumerate}[$+$]
  \item Faster if fewer ranks involved in the subset 
\end{enumerate}
{
\lstset{basicstyle=\ttfamily\scriptsize}
Virtual Topologies: 
\begin{enumerate}[\roman*.]
  \item Cartesian: \lstinline|MPI_Cart_create/get/coords/shift|, \lstinline|MPI_Cartdim_get| 
  \item Graph: \lstinline|MPI_Graph_create/get/neighbours_count/neighbours|, \lstinline|MPI_Graphdims_get|
\end{enumerate}

Common Collectives:
\begin{enumerate}[\roman*.]
  \item \lstinline|MPI_Barrier(MPI_Comm comm)|
  \item \lstinline|MPI_Bcast(void *buf, int count, MPI_Datatype dt, int root, MPI_Comm comm)|
  \item \lstinline|MPI_Scatter(void *sbuf, int scount, MPI_Datatype stype, void *rbuf, int rcount, MPI_Datatype rtype, int root, MPI_Comm comm)|
  \item \lstinline|MPI_Gather(void *sbuf, int scount, MPI_Datatype stype, void *rbuf, int rcount, MPI_Datatype rtype, int root, MPI_Comm comm)|
  \item \lstinline|MPI_Reduce(void *sbuf, void *rbuf, int count, MPI_Datatype dt, MPI_Op op, int root, MPI_Comm comm)|
  \item \lstinline|MPI_Allgather(void *sbuf, int scount, MPI_Datatype stype, void *rbuf, int rcount, MPI_Datatype rtype, MPI_Comm comm)|
  \item \lstinline|MPI_Reduce_scatter(void *sbuf, void *rbuf, int rcounts[], MPI_Datatype dt, MPI_Op op, MPI_Comm comm)|
  \item \lstinline|MPI_Alltoall(void *sbuf, int scount, MPI_Datatype stype, void *rbuf, int rcount, MPI_Datatype rtype, MPI_Comm comm)|
\end{enumerate}
}

\colbreak
\subsubsection{Basic MPI Program}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
int rank, size;

MPI_Init(&argc, &argv);

MPI_Comm_size(MPI_COMM_WORLD, &size);
MPI_Comm_rank(MPI_COMM_WORLD, &rank);
printf("Rank %d of %d\n", rank, size);

MPI_Finalize();
\end{lstlisting}

\subsubsection{Send and Receive}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
int msg;
MPI_Status status;

if (rank == 0) {
  msg = 42;
  MPI_Send(&msg, 1, MPI_INT, 1, 0, MPI_COMM_WORLD);
} else if (rank == 1) {
  MPI_Recv(&msg, 1, MPI_INT, 0, 0,
           MPI_COMM_WORLD, &status);
}
\end{lstlisting}

\subsubsection{Wildcard Receive}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
int msg;
MPI_Status status;

MPI_Recv(&msg, 1, MPI_INT,
         MPI_ANY_SOURCE, MPI_ANY_TAG,
         MPI_COMM_WORLD, &status);

printf("from %d tag %d\n",
       status.MPI_SOURCE, status.MPI_TAG);
\end{lstlisting}

\subsubsection{Non-Blocking Send/Receive}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
MPI_Request reqs[2];

MPI_Irecv(&recvbuf, 1, MPI_INT, src, 0,
          MPI_COMM_WORLD, &reqs[0]);

MPI_Isend(&sendbuf, 1, MPI_INT, dst, 0,
          MPI_COMM_WORLD, &reqs[1]);

MPI_Waitall(2, reqs, MPI_STATUSES_IGNORE);
\end{lstlisting}

\colbreak

\subsubsection{Deadlock Avoidance}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
MPI_Status status;

if (rank % 2 == 0) {
  MPI_Send(&sendbuf, 1, MPI_INT, right, 0, comm);
  MPI_Recv(&recvbuf, 1, MPI_INT, left, 0,
           comm, &status);
} else {
  MPI_Recv(&recvbuf, 1, MPI_INT, left, 0,
           comm, &status);
  MPI_Send(&sendbuf, 1, MPI_INT, right, 0, comm);
}
\end{lstlisting}

\subsubsection{Communicator Split}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
MPI_Comm subcomm;
int color = rank % 2;
int subrank;

MPI_Comm_split(MPI_COMM_WORLD, color, rank, &subcomm);
MPI_Comm_rank(subcomm, &subrank);

/* communicate within subcomm */

MPI_Comm_free(&subcomm);
\end{lstlisting}

\subsubsection{Cartesian Topology}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
MPI_Comm cart;
int dims[2] = {rows, cols};
int periods[2] = {0, 0};

MPI_Cart_create(MPI_COMM_WORLD, 2, dims,
                periods, 0, &cart);

MPI_Cart_shift(cart, 0, 1, &up, &down);
MPI_Cart_shift(cart, 1, 1, &left, &right);

MPI_Comm_free(&cart);
\end{lstlisting}

\subsubsection{Broadcast}
\begin{center}
  \incimg[0.5]{broadcast}
\end{center}
\colbreak

\subsubsection{Scatter and Gather}
\begin{center}
  \incimg[0.5]{scattergather}
\end{center}

\subsubsection{Reduce}
\begin{center}
  \incimg[0.4]{reduce}
\end{center}

\subsubsection{Allgather}
\begin{center}
  \incimg[0.5]{allgather}
\end{center}

\subsubsection{Reduce Scatter}
\begin{center}
  \incimg[0.6]{reducescatter}
\end{center}

\subsubsection{All-to-All}
\begin{center}
  \incimg[0.6]{alltoall}
\end{center}

\colbreak
\section{Performance}
Performance is measured by primary goals:
\begin{enumerate}[\roman*.]
  \item Response Time: wall-clock duration of an execution\\ 
    consists of user, system and waiting time
  \item Throughput: average work completed per unit time
\end{enumerate}

\subsection{Sequential Programs}
User CPU Time:
\begin{gather*}
  T_{user} = N_{cycle}\times T_{cycle}
\end{gather*}
\begin{enumerate}[\roman*.]
  \item $ T_{cycle} = \frac{1}{\text{(average) clock freq.}}$
  \item $ N_{cycle} = \sum n_i\times CPI_i\approx\; N_{instr}\times CPI$
\end{enumerate}\vspace{1em}

User CPU Time (Memory Access; One-level Cache):
\begin{gather*}
  T_{user} = \bigl(N_{cycle} + N_{mm\_cycle}\bigr)\times T_{cycle}
\end{gather*}
\begin{enumerate}[\roman*.]
  \item $ N_{mm\_cycle} = N_{read\_cycle} + N_{write\_cycle}$
  \item $ N_{read\_cycle} = N_{reads}\times R_{miss\_rate}\times N_{miss\_cycles}$
\end{enumerate}\vspace{1em}

Average Memory Access Time (Two-level Cache):
\begin{gather*}
  T_{read} = T_{hit} + \bigl(R_{miss}\times T_{miss}\bigr)
\end{gather*}
\begin{enumerate}[\roman*.]
  \item $ T_{read} = T^{L1}_{hit} + \bigl(R^{L1}_{miss}\times T^{L1}_{miss}\bigr)$
  \item $ T^{L1}_{miss} = T^{L2}_{hit} + \bigl(R^{L2}_{miss}\times T^{L2}_{miss}\bigr)$
  \item Global miss rate: $R_{miss} = R^{L1}_{miss}\times R^{L2}_{miss}$
\end{enumerate}\vspace{1em}

User CPU Time (Integrated CPI + Memory):
\begin{gather*}
  T_{user} =
  \Bigl(N_{cycle} + [N_{rw\_ops}\times R_{miss}\times N_{miss\_cycles}]\Bigr)
  \times T_{cycle}
\end{gather*}

Throughput Metrics:
\begin{enumerate}[\roman*.]
  \item $\text{MIPS} = \frac{N_{instr}}{T_{user} \times 10^6}$ 
  \item $\text{MFLOPS} = \frac{N_{fl\_ops}}{T_{user} \times 10^6}$
  \begin{enumerate}[leftmargin=*, label=$+$]\vspace{5pt}
    \item Restrict to float ops (but variance exists)
  \end{enumerate}
  \item $\text{Arithmetic Intensity (AI)} = \frac{N_{fl\_ops}}{N_{bytes\_rw}}$
  \begin{itemize}[leftmargin=*, label=$+$]\vspace{3pt}
    \item Roofline: $P \le \min\!\big(P_{\text{peak}},\; B\cdot \text{AI}\big)$ shows if bound by computation or communication bandwidth
  \end{itemize}
\end{enumerate}

\colbreak
\subsection{Parallel Programs}
Parallel Execution Time on $p$ PUs, $T_p$ consists:
\begin{enumerate}[\roman*.]
  \item Time for executing local computations
  \item Time for exchange of data between PUs
  \item Time for synchronization between PUs
  \item Waiting time
\end{enumerate}\vspace{1em}

Speedup:
\begin{gather*}
  S_p = \frac{T_{*}}{T_p}
\end{gather*}
\begin{enumerate}[\roman*.]
  \item Measures the benefit of parallelism compared to best sequential algorithm time $T_*$ 
  \item Theoretically, $S_p \le p$ always holds
  \item In practice, $S_p > p$ (superlinear speedup) can occur (e.g. problem working set ``fits'' in the cache)
\end{enumerate}\vspace{1em}

Cost:
\begin{gather*}
  C_p = p \times T_p
\end{gather*}
\begin{enumerate}[\roman*.]
  \item Measures total work performed by all PUs\\ 
    i.e. processor-runtime product
\item Cost-optimal: parallel program executes same total number of operations as fastest sequential program
\end{enumerate}\vspace{1em}

Efficiency:
\begin{gather*}
  E_p = \frac{T_{*}}{C_p} = \frac{S_p}{p} = \frac{T_{*}}{p \times T_p}
\end{gather*}
\begin{enumerate}[\roman*.]
  \item Measures actual degree of speedup performance achieved compared to maximum
  \item Theoretically, ideal $S_p=p \;\Rightarrow\; E_p=1$
  \item In practice, $E_p > 1$ can occur with superlinear speedups
\end{enumerate}\vspace{1em}

\colbreak
\subsection{Scalability}
Scalability is the interaction between problem size and parallel machine size:
\begin{enumerate}[\roman*.]
  \item Impacts: load balancing, overhead, arithmetic intensity, locality (application dependent)
  \item Fixed problem size:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Small $n$: overheads dominate $\Rightarrow$ suitable for small machine, poor on large machine
      \item Large $n$: working set may not fit on small machine (thrashing / exceeds cache / can't run)
    \end{itemize}
  \item Constraints:
    \begin{itemize}[leftmargin=*]
      \item Application: particles/processor (N-body), transactions/processor (distributed DB) 
      \item Resources: time-limited, memory-limited, problem-limited (same problem faster)
    \end{itemize}
\end{enumerate}

Amdahl's Law:
\begin{gather*}
  S_p(n) = \frac{T_*(n)}{f \cdot T_*(n) + \frac{1-f}{p}T_*(n)}
        = \frac{1}{f + \frac{1-f}{p}}
        \le \frac{1}{f}
\end{gather*}
\begin{enumerate}[\roman*.]
  \item Sequential fraction: $0 \leq f \leq 1$ limits speedup
  \item Discourage from making large parallel computers, focus on reducing sequential fraction $f$
  \item Rebuttal: circumvented for large problem size 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Commonly $f$ is a function of $n$, $f(n)$
      \item Efficient parallel algorithm: $\lim_{n\rightarrow\infty} f(n)=0$
      \item Thus, $\lim_{n\rightarrow\infty}S_p = \frac{p}{1 + (p-1)f(n)} = p$ 
    \end{itemize}
\end{enumerate}

Gustafson's Law:
\begin{gather*}
  S_p(n) = \frac{\tau_f + \tau_v(n,1)}{\tau_f + \tau_v(n,p)} = f + (1-f)p
\end{gather*}
\begin{enumerate}[\roman*.]
  \item Encouraged to use more processors to solve larger problem in the same time (in CPU-bound apps)
  \item $\tau_f$: constant sequential execution time 
  \item $\tau_v(n,p)$: parallel execution time on $p$ processors;\\if perfectly parallelizable (no overheads):
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item $\tau_v(n,1)=T^*(n)-\tau_f$
      \item $\tau_v(n,p)=\frac{T^*(n)-\tau_f}{p}$
    \end{itemize}
  \item If $T^*(n)$ increases strongly monotonically with $n$, then $\lim_{n\rightarrow\infty} S_p(n)=p$
\end{enumerate}

\subsection{Instrumentation}
Performance Instrumentation finds performance metrics:
\begin{enumerate}[\roman*.]
  \item Latency: time to service a request 
  \item Response Time: wall-clock duration of an execution
  \item Throughput: average work completed per unit time
  \item IOPS: I/O operations per second
  \item Utilization: fraction of time a resource is busy
  \item Saturation: degree to which a resource has queued work it cannot service 
\end{enumerate}

Perspectives:
\begin{enumerate}[\roman*.]
  \item Resource Analysis: focus on utilization
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Time-based: average time the resource was busy 
      \item Capacity-based: resource throughput
    \end{itemize}
  \item Workload Analysis: focus on throughput, latency
\end{enumerate}

Methodologies:
\begin{enumerate}[\roman*.]
  \item Anti-Methodologies: undeliberate, street light (look for obvious issues), drunk man (tune at random)
  \item Problem Statement
  \item USE (per resource): Utilization, Saturation, Errors
  \item Monitoring: record metrics over time
\end{enumerate}

Tool Types \& Counters:
\begin{enumerate}[\roman*.]
  \item Observability: watch activity under workload (timing statements, performance counters)
  \item Static: examine system at rest
  \item Benchmarking: load test (caution: contention/production risk)
  \item Tuning: change defaults (danger: regressions now/later under load)
  \item Categorization:
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item System-wide vs per-process
      \item Fixed counters (kernel-maintained metrics) vs event-based counters (enabled as needed)
      \item Profiling: samples/snapshots \quad vs \quad Tracing: record every event occurrence
    \end{itemize}
\end{enumerate}\vspace{0.5em}

\colbreak

\incimg[0.9]{linux_observability_tools}
\incimg[0.9]{linux_static_tools}
\incimg[0.9]{linux_benchmarking_tools}

\colbreak

\colbreak
\section{Energy-Efficient Computing}

Motivation:
\begin{enumerate}[\roman*.]
  \item Modern compute uses too much power
  \item Higher performance $\Rightarrow$ more/faster computers $\Rightarrow$ more power, heat, cooling, space, cost
\end{enumerate}

Energy and Power:
\begin{enumerate}[\roman*.]
  \item Energy $E$ [Joules (J)]: capacity for doing work
  \item Power $P$ [Watts = J/s]: energy transferred/converted per unit time
    \[
      P = \frac{E}{t}
    \]
  \item Higher energy consumption costs more
  \item Almost $\sim$100\% processor power becomes heat
\end{enumerate}

\subsection{Power Wall}

Dennard Scaling (1974):
\begin{enumerate}[\roman*.]
  \item Smaller transistors should use same power per unit area
  \item Implies more compute at same power density
  \item Ended around $\sim$2005: smaller transistors needed more power per unit area
\end{enumerate}

Power Wall Problem:
\begin{enumerate}[\roman*.]
  \item Cannot fit more transistors into same area without increasing power density and needing more cooling
  \item Cannot keep increasing frequency:\\more frequency $\Rightarrow$ more heat
  \item Processors are power-limited by thermal constraints
  \item Need to make better use of available power
\end{enumerate}

\colbreak
\subsection{Per-Processor Efficiency}

Performance-per-Watt:
\[
  \text{Performance-per-Watt}
  =
  \frac{\text{Benchmark Score}}{\text{Processor Power}}
\]
\begin{enumerate}[\roman*.]
  \item Depends on workload, benchmark, settings, and absolute score
  \item Performance-per-watt is not constant across frequency/power levels
  \item Often does not increase linearly with power
\end{enumerate}

Processor Power Model:
\begin{align*}
  P_{total} &= P_{dynamic} + P_{static}\\
  P_{dynamic} &= kV^2f
\end{align*}
Implications:
\begin{enumerate}[\roman*.]
  \item $P_{dynamic}$: power used to perform operations
  \item $P_{static}$: power used independent of useful work
  \item Voltage $V$ has quadratic impact on dynamic power
  \item Frequency $f$ has linear impact and impacts performance score
  \item Voltage and frequency are not independent:\\higher frequencies require higher voltages
\end{enumerate}

Dynamic Voltage and Frequency Scaling (DVFS):
\begin{enumerate}[\roman*.]
  \item Modern processors adjust voltage and clock frequency dynamically
  \item Idle/simple tasks need less performance\\$\Rightarrow$ reduce frequency and voltage\\$\Rightarrow$ reduces power usage and heat
  \item Each frequency has a minimum safe voltage for correct operation
\end{enumerate}

Heterogeneous Cores:
\begin{enumerate}[\roman*.]
  \item Use different core types with different power-performance curves
  \item Scheduler chooses core type based on workload demand
\end{enumerate}

\colbreak

\subsection{Datacenter Efficiency}

Efficiency Metrics:
\begin{enumerate}[\roman*.]
  \item GFLOPs-per-Watt: compute efficiency for large benchmarks
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item Green500 ranks supercomputers
      \item Measures compute power efficiency, not full facility overhead
    \end{itemize}
  \item Power Usage Effectiveness:
    \begin{align*}
      \text{PUE}
      &=
      \frac{\text{Total Facility Energy}}{\text{IT Equipment Energy}}\\
      &=
      1 +
      \frac{\text{Non-IT Facility Energy}}{\text{IT Equipment Energy}}
    \end{align*}
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item IT energy: servers, networking, storage
      \item Non-IT energy: cooling, lighting, pumps, power conversion
      \item Lower PUE means less overhead energy
    \end{itemize}
\end{enumerate}

Issues with PUE:
\begin{enumerate}[\roman*.]
  \item Hot climates are disadvantaged
  \item Larger compute loads can reduce PUE while increasing absolute energy use
  \item Operators may be incentivized to measure fewer overheads
  \item PUE does not measure computational efficiency directly
\end{enumerate}

Reducing Datacenter Energy:
\begin{enumerate}[\roman*.]
  \item Improve compute efficiency:
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item Integrated accelerators
      \item High-bandwidth CPU--GPU links (e.g. Grace--Hopper)
      \item Unified memory / less data movement
    \end{itemize}
  \item Improve facility efficiency:
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item Aisle containment: separate hot/cold air paths
      \item Warm-water cooling: directly cool CPUs/GPUs
    \end{itemize}
\end{enumerate}
\colbreak 
\section*{Appendix}
\begin{minipage}{0.48\linewidth}
\centering
\begin{tabular}{|l c c|}
\hline
\textbf{name} & \textbf{prefix} & \textbf{multiplier} \\
\hline
exa  & E  & $10^{18}$ \\
peta & P  & $10^{15}$ \\
tera & T  & $10^{12}$ \\
giga & G  & $10^{9}$  \\
mega & M  & $10^{6}$  \\
kilo & K  & $10^{3}$  \\
\hline
milli & m  & $10^{-3}$ \\
micro & $\mu$ & $10^{-6}$ \\
nano  & n  & $10^{-9}$ \\
pico  & p  & $10^{-12}$ \\
\hline
\end{tabular}
\end{minipage}\hfill
\begin{minipage}{0.48\linewidth}
\centering
\begin{tabular}{|l c c|}
\hline
\textbf{name} & \textbf{prefix} & \textbf{multiplier} \\
\hline
exbi & Ei & $2^{60}$ \\
pebi & Pi & $2^{50}$ \\
tebi & Ti & $2^{40}$ \\
gibi & Gi & $2^{30}$ \\
mebi & Mi & $2^{20}$ \\
kibi & Ki & $2^{10}$ \\
\hline
\end{tabular}
\end{minipage}

\subsection{Parbegin--Parend Syntax}

Parbegin--parend explicitly specifies parallel regions:
\begin{enumerate}[\roman*.]
  \item \lstinline|parbegin|: start of parallel/concurrent region
  \item \lstinline|parallel|: separates statements that run concurrently
  \item \lstinline|parend|: end of parallel region; spawned threads join
  \item Statements without \lstinline|parallel| separation execute sequentially
\end{enumerate}

\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
parbegin
  do
    parbegin
      A
      parallel
      B
    parend
    C
  parallel
  D
parend
E
\end{lstlisting}

\begin{enumerate}[\roman*.]
  \item Inner: \lstinline|A| and \lstinline|B| run concurrently
  \item \lstinline|C| waits for inner \lstinline|A| and \lstinline|B| to complete
  \item Outer: \lstinline|D| runs concurrently with the inner block
  \item \lstinline|E| waits for the entire outer region to complete
\end{enumerate}

\colbreak
\subsection{Special Constants}
OpenMP Reduction Operations:
\begin{enumerate}[\roman*.]
  \item Arithmetic: \lstinline|+|, \lstinline|-|, \lstinline|*|
  \item Logical: \lstinline|&&|, \lstinline{||}
  \item Bitwise: \lstinline|&|, \lstinline{|}, \lstinline|^|
  \item Min/Max: \lstinline|min|, \lstinline|max|
\end{enumerate}

MPI Types:
\begin{enumerate}[\roman*.]
  \item Integer: \lstinline|MPI_INT|, \lstinline|MPI_LONG|, \lstinline|MPI_LONG_LONG|
  \item Floating-point: \lstinline|MPI_FLOAT|, \lstinline|MPI_DOUBLE|, \lstinline|MPI_LONG_DOUBLE|
  \item Character: \lstinline|MPI_CHAR|, \lstinline|MPI_BYTE|
  \item Unsigned: \lstinline|MPI_UNSIGNED|, \lstinline|MPI_UNSIGNED_LONG|
  \item Logical: \lstinline|MPI_C_BOOL|, \lstinline|MPI_CXX_BOOL|
  \item Custom: built using \lstinline|MPI_Type_create_*|, then committed with \lstinline|MPI_Type_commit|
\end{enumerate}

MPI Reduction Operations:
\begin{enumerate}[\roman*.]
  \item \lstinline|MPI_MAX|. \lstinline|MPI_MIN|
  \item \lstinline|MPI_SUM|, \lstinline|MPI_PROD|
  \item Logical: \lstinline|MPI_LAND|, \lstinline|MPI_LOR|, \lstinline|MPI_LXOR|
  \item Bitwise: \lstinline|MPI_BAND|, \lstinline|MPI_BOR|, \lstinline|MPI_BXOR|
  \item Locate: \lstinline|MPI_MAXLOC|, \lstinline|MPI_MINLOC|
\end{enumerate}

MPI Wildcards / Constants:
\begin{enumerate}[\roman*.]
  \item \lstinline|MPI_COMM_WORLD|: default communicator
  \item \lstinline|MPI_ANY_SOURCE|: receive from any source
  \item \lstinline|MPI_ANY_TAG|: receive with any tag
  \item \lstinline|MPI_STATUS_IGNORE|, \lstinline|MPI_STATUSES_IGNORE|: ignore status output
\end{enumerate}

\colbreak 

\subsection{Shear Sort}

Shear sort sorts $N$ values on a $\sqrt{N}\times\sqrt{N}$ 2D mesh of processing elements. Shear sort is suited to a 2D mesh because communication is mostly row-wise/column-wise between neighbouring processing elements.

Layout:
\begin{enumerate}[\roman*.]
  \item One value per processing element
  \item Final order is snake-like:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Odd rows ascending
      \item Even rows descending
      \item Columns ascending
    \end{itemize}
\end{enumerate}

Algorithm:
\begin{enumerate}[\roman*.]
  \item Row sort: odd rows ascending, even rows descending
  \item Column sort: all columns ascending
  \item Repeat row/column phases until sorted
  \item Finish with a row sort
\end{enumerate}

Complexity:
\begin{enumerate}[\roman*.]
  \item Number of phases: $\log_2 N + 1$
  \item Each row/column sort can use odd-even transposition sort
  \item Each phase takes $\sqrt{N}$ parallel steps
  \item Total parallel steps:
    \[
      \sqrt{N}(\log_2 N + 1)
    \]
\end{enumerate}



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                       End                          %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\end{multicols*}
\end{document}

\documentclass[12pt, a4paper]{article}

\input{preamble}
\input{preamble-cheatsheet}
\input{letterfonts}

\newcommand{\mytitle}{CS3211 Concurrent Programming}
\newcommand{\myauthor}{github/omgeta}
\newcommand{\mydate}{AY 25/26 Sem 2}

\begin{document}
\raggedright
\footnotesize
\begin{multicols*}{3}
\setlength{\premulticols}{1pt}
\setlength{\postmulticols}{1pt}
\setlength{\multicolsep}{1pt}
\setlength{\columnsep}{2pt}

{\normalsize{\textbf{\mytitle}}}\\
{\footnotesize{\mydate\hspace{2pt}\textemdash\hspace{2pt}\myauthor}}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                      Begin                         %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Introduction}
Concurrency refers to a system performing multiple independent tasks in overlapping time periods.

Concurrency vs. Parallelism:
\begin{enumerate}[\roman*.]
  \item Concurrency: tasks progress in overlapping time 
  \item Parallelism: tasks also execute simultaneously 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Enabled by multiple cores, each can have multiple hardware threads (SMT) 
      \item Hardware threads dictate parallel execution of kernel threads
    \end{itemize}
\end{enumerate}

Program Parallelization Steps:
\begin{enumerate}[\roman*.]
  \item Decomposition: split problem into parallel tasks
    \begin{itemize}[leftmargin=*]\vspace{2pt}
      \item Granularity: size of task
    \end{itemize}
  \item Scheduling: assign tasks to threads
  \item Orchestration: synchronisation and communication of tasks to satisfy dependencies
  \item Mapping: bind threads to physical cores 
\end{enumerate}

Software Stack:
\begin{center}
  \incimg[0.7]{stack}
\end{center}

\colbreak
\subsection{Processes}
Process is an abstraction for a running program:
\begin{enumerate}[$-$]\vspace{-2pt}
  \item High system call overhead 
  \item OS allocates and initializes data structures 
  \item Communication costly through the OS
\end{enumerate}\vspace{-1pt}
{\centering
  \incimg[.8]{process}
\par}
\subsection{Threads}
Threads are independent execution flows within a process:
\begin{enumerate}[\roman*.]
  \item Shared: Resources, Address Space 
  \item Private: Thread ID, Registers, "Stack" (diff. SP)
\end{enumerate}\vspace{-1pt}
\begin{enumerate}[$+$]
  \item Efficient: cheaper creation and context switching
  \item Resource Sharing: process resources can be shared
\end{enumerate}\vspace{-1pt}
{\centering
  \incimg[0.7]{thread}
\par}
Implementations:
\begin{enumerate}[\roman*.]
  \item User-Threads are managed by a userspace library:
    \begin{enumerate}[$+$]\vspace{3pt}
    \item Fast: context switches have low overhead
  \end{enumerate}
  \begin{enumerate}[$-$]\vspace{5pt}
    \item Cannot parallelize to different cores 
    \item Threads blocking will block whole process
  \end{enumerate}
\item Kernel-Threads are implemented in the OS:
  \begin{enumerate}[$+$]\vspace{3pt}
    \item Parallel: threads can run across many cores 
  \end{enumerate}
  \begin{enumerate}[$-$]\vspace{5pt}
    \item Slower: thread operations are system calls
  \end{enumerate}
\end{enumerate}
\vspace{-1em}
\colbreak 
\subsection{Synchronisation}

Race Condition:
\begin{enumerate}[\roman*.]
  \item Program outcome depends on timing/ordering of concurrent operations
\end{enumerate}

Data Races lead to undefined behaviour:
\begin{enumerate}[\roman*.]
  \item Two concurrent access to same memory location
  \item At least one access is a write
  \item No synchronization
\end{enumerate}

Mutual Exclusion synchronises access to shared resources:
\begin{enumerate}[\roman*.]
  \item Thread locks mutex to access shared resources 
  \item Same thread unlocks mutex once shared resources no longer used
\end{enumerate}

Symptoms of Incorrect Synchronization:
\begin{enumerate}[\roman*.]
  \item Deadlock: there is an execution where no progress is made when it should be possible 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Indistinguishable from livelock 
    \end{itemize}
  \item Starvation: a thread cannot progress because other threads hold a required resource, which it does not get a chance to acquire
\end{enumerate}

\lstset{style=c}
Mechanisms:
\begin{enumerate}[\roman*.]
  \item Lock: primitive \lstinline|acquire()|, \lstinline|release()|
  \item Semaphore: atomic $\geq 0$; \lstinline|acquire()|, \lstinline|release()|   
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Binary: represents single access 
      \item Counting: number of threads in CS $\leq N$
      \item Can be manipulated by different threads 
      \item No relation to data being controlled
    \end{itemize}
  \item Monitors: high-level implicit concurrency
  \item Messages: explicit model of communication and synchronisation based on atomic data transfer
\end{enumerate}
\vspace{-1em}
\colbreak
\section{C++}
\lstset{style=cpp}
\subsection{Threads}

Operations:
\begin{enumerate}[\roman*.]
  \item Construct:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Function: \lstinline|std::thread t(f, args...)|
      \item Callable object: \lstinline|std::thread t{F{}, args...}|
      \item Lambda: \lstinline|std::thread t([]{ ... })|
    \end{itemize}
  \item Wait Once: \lstinline|t.join()|
    \begin{enumerate}[leftmargin=*, label=$\bullet$]\vspace{3pt}
      \item Check: \lstinline|t.joinable()|
      \item Must still join on exception paths 
    \end{enumerate}
  \item Detach: \lstinline|t.detach()|
    \begin{enumerate}[leftmargin=*, label=$-$]\vspace{3pt}
      \item Detached thread no longer joinable
      \item May outlive local variable references 
    \end{enumerate}
  \item Pass By Reference: \lstinline|std::ref(x)|
  \item Ownership: movable, not copyable
\end{enumerate}

\subsection{Mutexes}

Operations:
\begin{enumerate}[\roman*.]
  \item Construct: \lstinline|std::mutex m|
  \item Explicit: \lstinline|m.lock()|, \lstinline|m.unlock()|
  \begin{enumerate}[leftmargin=*, label=$-$]\vspace{3pt}
    \item Must unlock on every exit path, incl. exceptions
  \end{enumerate}
  \item RAII: \lstinline|std::lock_guard<std::mutex> g(m)| 
  \begin{enumerate}[leftmargin=*, label=$+$]\vspace{3pt}
    \item Locks on construction and unlocks on scope exit
  \end{enumerate}
  \item Flexible: \lstinline|std::unique_lock<std::mutex> lk(m)| 
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item Adopt Existing Lock: \lstinline|lk(m, std::adopt_lock)|
    \item Defer Lock: \lstinline|lk(m, std::defer_lock)|
  \end{itemize}
  \begin{enumerate}[leftmargin=*, label=$+$]\vspace{5pt}
    \item Unlocks on scope exit if mutex owned
    \item Allows multiple lock/unlock within a scope 
    \item Needed for condition variables
    \item Moveable: can transfer lock ownership
  \end{enumerate}
  \item Multiple Mutexes (Deadlock-Free):
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Lock in total order, avoid nested locks
      \item Explicit: \lstinline|std::lock(m1, m2, ...)| 
      \item RAII: \lstinline|std::scoped_lock lk(m1, m2, ...)| 
    \end{itemize}
\end{enumerate}
\vspace{-1em}
\colbreak
\subsection{Condition Variables}

Condition variables sync actions without busy-waiting.

Operations:
\begin{enumerate}[\roman*.]
  \item {Construct: \lstinline|std::condition_variable cv|} 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Works only on \lstinline|std::unique_lock<std::mutex>|
      \item Generic (any mutex-like, less efficient):\\\lstinline|std::condition_variable_any cva|
    \end{itemize}
  \item Wait: \lstinline|cv.wait(lk)| / \lstinline|cv.wait(lk, pred)| 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Checks predicate with mutex locked
      \item If condition is met, returns
      \item If condition is unmet, unlocks mutex and blocks
    \end{itemize}
    \begin{enumerate}[leftmargin=*, label=$-$]\vspace{5pt}
      \item Spurious Wake: may wake without notification to check condition; so should be side effect-free
    \end{enumerate}
  \item Wake One Thread: \lstinline|cv.notify_one()|
  \item Wake All Threads: \lstinline|cv.notify_all()|
\end{enumerate}

\subsection{Monitors}
Monitors encapsulate shared data and operations enforced by a mutex and condition variables.
\begin{enumerate}[\roman*.]
  \item One thread executes monitor operations at a time 
  \item Threads wait for conditions and state changes
\end{enumerate}
Example:
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
class JobQueue {
  std::mutex m;
  std::condition_variable cv;
  std::queue<Job> q;
public:
  void enqueue(Job j) {
    {
      std::unique_lock<std::mutex> lk(m);
      q.push(j);
    }
    cv.notify_one();
  }

  Job dequeue() {
    std::unique_lock<std::mutex> lk(m);
    cv.wait(lk, [&]{ return !q.empty(); });
    Job j = q.front(); q.pop();
    return x;
  }
};
\end{lstlisting}
\vspace{-1em}
\colbreak
\subsection{Memory Model}
Memory model is the contract about the visible side effects of memory operations.
\begin{enumerate}[\roman*.]
  \item As-if: compiler/hardware may reorder operations if single-threaded observable behavior is unchanged.
\end{enumerate}

Ordering Relations:
\begin{enumerate}[\roman*.]
  \item Sequenced-Before: program order
  \item Synchronizes-With: suitably-tagged store $\to$ load on same atomic, if the load reads that value 
  \item Happens-Before: transitive closure of Sequenced-Before $\cup$ Synchronizes-With 
  \item Modification Order: per-atomic total order of writes consistent across threads 
  \item Coherence Order: per-atomic partial order of writes (totally ordered) and reads (unordered w.r.t others) consistent across threads
  \item Visible Side-Effect: write visible to a read 
\end{enumerate}

Atomics (\lstinline|std::atomic<T>|) have indivisible operations:
\begin{enumerate}[\roman*.]
  \item Load: \lstinline|a.load(std::memory_order)|
  \item Store: \lstinline|a.store(val, std::memory_order)| 
  \item RMW: \lstinline|a.fetch_add(val, std::memory_order)|, \lstinline|a.exchange(val, std::memory_order)|
  \item May be lock-free/implemented with mutex:\\
    \lstinline|a.is_lock_free()|, \lstinline|T::is_always_lock_free|
\end{enumerate}

Memory Models:
\begin{enumerate}[\roman*.]
  \item Sequentially Consistent (\lstinline|seq_cst|):
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item All threads agree on same order of operations
    \end{itemize}
  \item Relaxed (\lstinline|relaxed|):
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item No Synchronizes-With cross-thread guarantees
    \end{itemize}
  \item Acquire-Release (\lstinline|acquire|, \lstinline|release|, \lstinline|acq_rel|)
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Release store $\rightarrow$ Acquire load on same atomic can establish Synchronizes-With
      \item Writes before release become visible after the acquire (in the acquiring thread)
      \item Release Sequence: chain of RMWs on the same atomic after release; an acquire reading from the chain synchronizes-with the original release
    \end{itemize}
\end{enumerate}
\vspace{-1em}
\colbreak
\subsection{Lock-based Data Structures}
\lstset{style=cpp}
Lock-based structures protect shared state with mutexes.
\begin{enumerate}[\roman*.]
  \item Lock the right mutex before accessing protected data
  \item Hold locks for the minimum time
  \item Interface should not expose race conditions
  \item Deadlocks are possible
\end{enumerate}

\subsubsection{Stack (one global mutex)}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
void push(T x) {
  std::lock_guard<std::mutex> lk(m);
  data.push(std::move(x));
}

std::shared_ptr<T> pop() {
  std::lock_guard<std::mutex> lk(m);
  if (data.empty()) throw empty_stack();
  auto res = std::make_shared<T>(std::move(data.top()));
  data.pop();
  return res;
}
\end{lstlisting}\vspace{-1em}
\begin{enumerate}[$+$]
  \item Safe for concurrent calls, exception-safe
\end{enumerate}
\begin{enumerate}[$-$]\vspace{-2pt}
  \item Work is serialized
  \item No built-in waiting for items: must poll \lstinline|empty()| or catch \lstinline|empty_stack| exceptions
\end{enumerate}

\subsubsection{Queue (one mutex + notification)}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
void push(T x) {
  std::lock_guard<std::mutex> lk(mut);
  data_queue.push(std::move(x));
  data_cond.notify_one();
}

void wait_and_pop(T& value) {
  std::unique_lock<std::mutex> lk(mut);
  data_cond.wait(lk, [this]{ return !data_queue.empty(); });
  value = std::move(data_queue.front());
  data_queue.pop();
}
\end{lstlisting}\vspace{-1em}
\begin{enumerate}[$+$]
  \item \lstinline|wait_and_pop()| blocks until an item is available
  \item Safe for concurrent member calls
\end{enumerate}
\begin{enumerate}[$-$]\vspace{-2pt}
  \item One mutex still serializes queue operations
  \item \lstinline|notify_one()| + exception in \lstinline|wait_and_pop()| can leave other waiters asleep
\end{enumerate}
\colbreak
\subsubsection{Queue (push shared-pointers in the queue)}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
void push(T x) {
  auto data = std::make_shared<T>(std::move(x));
  std::lock_guard<std::mutex> lk(mut);
  data_queue.push(data);
  data_cond.notify_one();
}

std::shared_ptr<T> wait_and_pop() {
  std::unique_lock<std::mutex> lk(mut);
  data_cond.wait(lk, [this]{ return !data_queue.empty(); });
  auto res = data_queue.front();
  data_queue.pop();
  return res;
}
\end{lstlisting}\vspace{-1em}
\begin{enumerate}[$+$]
  \item Data can be allocated before locking
  \item Returned value can share ownership with the queue element
\end{enumerate}
\begin{enumerate}[$-$]\vspace{-2pt}
  \item One mutex still serializes queue operations
\end{enumerate}

\subsection{Queue (fine-grained; dummy node)}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
node* get_back() {
  std::lock_guard<std::mutex> lk(back_mutex);
  return back;
}

std::unique_ptr<node> pop_front() {
  std::lock_guard<std::mutex> lk(front_mutex);
  if (front.get() == get_back()) return nullptr;
  auto old_front = std::move(front);
  front = std::move(old_front->next);
  return old_front;
}

void push(T x) {
  auto new_data = std::make_shared<T>(std::move(x));
  auto p = std::make_unique<node>();
  node* new_back = p.get();
  std::lock_guard<std::mutex> lk(back_mutex);
  back->data = new_data;
  back->next = std::move(p);
  back = new_back;
}
\end{lstlisting}\vspace{-1em}
\begin{enumerate}[$+$]
  \item Separate \lstinline|front_mutex| and \lstinline|back_mutex|
  \item \lstinline|push()| only locks the back
  \item More concurrency than one global mutex
\end{enumerate}
\begin{enumerate}[$-$]\vspace{-2pt}
  \item Locking protocol and empty-check become more complex
\end{enumerate}
\vspace{-1em}
\colbreak
\subsubsection{Queue (more fine-grained)}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
std::unique_ptr<node> try_pop_head() {
  std::unique_lock<std::mutex> front_lk(front_mutex);
  if (front.get() == get_back()) return nullptr;

  std::unique_lock<std::mutex> node_lk(front->mutex);
  auto old_head = std::move(front);
  front = std::move(old_head->next);
  return old_head;
}

std::shared_ptr<T> try_pop() {
  std::unique_ptr<node> old_head;
  { old_head = try_pop_head(); }   // lock released before delete
  return old_head ? old_head->data : std::shared_ptr<T>();
}

void push(T x) {
  auto new_data = std::make_shared<T>(std::move(x));
  auto p = std::make_unique<node>();
  node* new_back = p.get();

  std::lock_guard<std::mutex> back_lk(back_mutex);
  std::lock_guard<std::mutex> node_lk(back->mutex);
  back->data = new_data;
  back->next = std::move(p);
  back = new_back;
}
\end{lstlisting}\vspace{-1em}
\begin{enumerate}[$+$]
  \item Per-node mutex
  \item More fine-grained synchronization between \lstinline|push()| and \lstinline|pop()|
\end{enumerate}
\begin{enumerate}[$-$]\vspace{-2pt}
  \item More complex locking protocol
  \item Must release locks before destroying the old node
\end{enumerate}
\colbreak

\subsection{Lock-Free Data Structures}
\lstset{style=cpp}

Lock-free structures avoid blocking library calls.
\begin{enumerate}[\roman*.]
  \item Obstruction-free: a thread completes if it eventually runs in isolation
  \item Lock-free: from any point, some thread completes
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Livelock-free but Starvation possible
    \end{itemize}
    \begin{enumerate}[leftmargin=*,label=$+$]\vspace{5pt}
      \item Maximum concurrency: some thread progresses
      \item Robust: if one thread dies, others can proceed
    \end{enumerate}
    \begin{enumerate}[leftmargin=*,label=$-$]\vspace{5pt}
      \item Atomics may be much slower than non-atomics
      \item Contention/write propagation hurt performance
      \item Cache ping-pong and false sharing
    \end{enumerate}
  \item Wait-free: every running thread completes regardless of execution speed of others
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Livelock- and Starvation-free; stronger lock-free
    \end{itemize}
    \begin{enumerate}[leftmargin=*,label=$+$]\vspace{5pt}
      \item Per-thread progress guaranteed
    \end{enumerate}
    \begin{enumerate}[leftmargin=*,label=$-$]\vspace{5pt}
      \item Harder to design and prove correct
      \item Often higher overhead than lock-free structures
    \end{enumerate}
\end{enumerate}

Risks:
\begin{enumerate}[\roman*.]
  \item Livelock: from unbounded restarts in response to other thread actions
  \item Starvation: loops with CAS, or algorithms with unbounded number of retries due to clashes
\end{enumerate}

Guidelines:
\begin{enumerate}[\roman*.]
  \item Start with \lstinline|std::memory_order_seq_cst|
  \item Use CAS loops carefully
  \item Watch for ABA
  \item Use safe memory reclamation
  \item Prefer lock-based unless lock-free progress needed
\end{enumerate}

\subsubsection{CAS Loop Pattern}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
int fetch_add(std::atomic<int>& value, int add) {
  int old_value = value.load();
  while (true) {
    int new_value = old_value + add;
    if (value.compare_exchange_weak(old_value, new_value))
      return old_value;
  }
}
\end{lstlisting}\vspace{-1em}

\subsubsection{Queue (initial idea)}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
void push(Job job) {
  Node* new_dummy = new Node();
  Node* work_node = m_queue_back.exchange(new_dummy);
  work_node->job = job;
  work_node->next.store(new_dummy);
}

std::optional<Job> try_pop() {
  Node* old_front = m_queue_front.load();
  while (true) {
    Node* new_front = old_front->next.load();
    if (new_front == QUEUE_END)
      return std::nullopt;
    if (m_queue_front.CAS(old_front, new_front))
      return old_front->job;
  }
}
\end{lstlisting}
\begin{enumerate}[$\bullet$]
  \item \lstinline|exchange()| gives each producer its exclusive dummy node
  \item \lstinline|try_pop()| advances the head with a CAS loop
\end{enumerate}

\subsubsection{Issue 1: Data races}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
Node* work_node = m_queue_back.exchange(new_dummy, stdmo::acq_rel);
work_node->next.store(new_dummy, stdmo::release);

Node* old_front = m_queue_front.load(stdmo::acquire);
Node* new_front = old_front->next.load(stdmo::acquire);
m_queue_front.compare_exchange_weak(old_front, new_front, stdmo::acq_rel);
\end{lstlisting}
\begin{enumerate}[$\bullet$]
  \item Producer vs.\ producer: \lstinline|m_queue_back.exchange(..., stdmo::acq_rel)|
  \item Producer vs.\ its consumer: release store to \lstinline|next|, acquire load from \lstinline|next|
  \item Producer vs.\ next consumer: acquire / acq\_rel accesses on \lstinline|m_queue_front|
\end{enumerate}

\colbreak
\subsubsection{Issue 2: ABA}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
struct alignas(16) GenNodePtr {
  Node* node;
  uintptr_t gen;
};

std::atomic<GenNodePtr> m_queue_front;
\end{lstlisting}\vspace{-1em}
\begin{enumerate}[$\bullet$]
  \item CAS may succeed even if a pointer changes away from A and back to A
  \item Fix: add a generation counter to the front pointer
\end{enumerate}

\subsubsection{Issue 3: Use-after-free}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
Node* get_recycled_node_or_allocate_new();
void add_node_to_recycling_stack(Node* node);
\end{lstlisting}
\begin{enumerate}[$\bullet$]
  \item Old front pointers may become stale
  \item Fix: do not delete nodes directly; reuse nodes via a lock-free recycling stack
\end{enumerate}

\subsubsection{Issue 4: Recycling race}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
Node* cur_stack_next =
  old_stack_top.node->next.load(stdmo::acquire);

node->next.store(old_stack_top.node, stdmo::release);
\end{lstlisting}
\begin{enumerate}[$\bullet$]
  \item Recycling also needs synchronization
  \item Use release / acquire on recycled-node \lstinline|next|
\end{enumerate}

\subsubsection{Issue 5: Serializability}
\begin{enumerate}[$\bullet$]
  \item The repaired queue can still be non-serializable: head / tail may be updated before fully linked
  \item Fix: Michael-Scott Queue prioritizes maintaining a valid linked list 
\end{enumerate}

\colbreak
\subsubsection{Classes}

Class Skeleton:
\begin{lstlisting}
class C {
  std::string x;
public:
  C(): x("default") {
    // default constructor
  }

  C(int i): x(std::string(i)) {
    // custom constructor
  }

  C(const C& other) : x(other.x) {
    // copy constructor
  }

  C& operator=(const C& other) {
    // copy assignment operator
    x = other.x;
    return *this;
  }

  C(C&& other): x(std::move(other.x)) {
    // move constructor
  }

  C& operator=(C&& other) {
    // move assignment operator
    x = std::move(other.x);
    return *this;
  }

  ~C() {
    // destructor
  }
};
\end{lstlisting}

Rule of 3: if a class needs a custom destructor, copy constructor, or copy assignment operator then it usually needs all 3

\colbreak
\subsubsection{Shared Mutex}
Readers-Writers:
\begin{lstlisting}
class ThreadSafeCounter {
    std::shared_mutex mutex_;
    unsigned int value_{};
public:
    ThreadSafeCounter() = default;

    // Multiple threads/readers can read the counter's value at the same time.
    unsigned int get() const
    {
        std::shared_lock lock(mutex_);
        return value_;
    }

    // Only one thread/writer can increment/write the counter's value.
    void increment()
    {
        std::unique_lock lock(mutex_);
        ++value_;
    }

    // Only one thread/writer can reset/write the counter's value.
    void reset()
    {
        std::unique_lock lock(mutex_);
        value_ = 0;
    }
};
\end{lstlisting}

\colbreak
\subsubsection{Smart Pointers}

Smart pointers manage heap resources automatically.

Ownership:
\begin{enumerate}[\roman*.]
  \item \lstinline|std::unique_ptr<T>|: exclusive ownership
  \item \lstinline|std::shared_ptr<T>|: shared ownership via atomic reference counting
  \item \lstinline|std::weak_ptr<T>|: non-owning observer of a \lstinline|shared_ptr|
\end{enumerate}

Basic Methods:
\begin{enumerate}[\roman*.]
  \item Construct:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item \lstinline|auto p = std::make_unique<T>(args...)|
      \item \lstinline|auto p = std::make_shared<T>(args...)|
    \end{itemize}
  \item Access:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Dereference: \lstinline|*p|, \lstinline|p->member|
      \item Raw pointer: \lstinline|p.get()|
    \end{itemize}
  \item Reset / replace:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item \lstinline|p.reset()|
      \item \lstinline|p.reset(new T(...))|
    \end{itemize}
  \item Move ownership:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item \lstinline|std::unique_ptr| is movable, not copyable
      \item \lstinline|auto q = std::move(p)|
    \end{itemize}
  \item Shared count:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item \lstinline|p.use_count()|
      \item Copying a \lstinline|shared_ptr| increments the count
    \end{itemize}
  \item Weak access:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item \lstinline|w.lock()| gives a \lstinline|shared_ptr| if object still exists
      \item \lstinline|w.expired()| checks whether object has already been destroyed
    \end{itemize}
  \item Unique-pointer release:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item \lstinline|p.release()| returns raw pointer and gives up ownership
    \end{itemize}
\end{enumerate}
\colbreak 
\section{Go}
\lstset{style=go}

\subsection{Goroutines}
Goroutines are lightweight function tasks running in the same address space.

Operations:
\begin{enumerate}[\roman*.]
  \item Start: \lstinline|go f(args...)|
  \item Lifetime:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Program exits when \lstinline|main| returns 
      \item Not GC'd: avoid goroutine leaks
    \end{itemize}
  \item Blocks only block the thread, no other goroutines
  \item Preemptable: by Go's runtime
\end{enumerate}

\subsection{Channels}
Channels coordinate goroutines by communication.

Operations:
\begin{enumerate}[\roman*.]
  \item Make: \lstinline|ch := make(chan T)| / \lstinline|make(chan T, C)|
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Directional:\\\lstinline|chan<- T| (send-only), \lstinline|<-chan T| (recv-only)
    \end{itemize}
  \item Send: \lstinline|ch <- v|
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Blocks if channel is full (buffered) /\\until a receiver  (unbuffered)
    \end{itemize}
  \item Receive: \lstinline|v, ok := <-ch|
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Blocks if channel is empty (can cause deadlocks)
      \item \lstinline|ok| false $\iff$ \lstinline|v| is default from closed channel
    \end{itemize}
  \item Close: \lstinline|close(ch)|
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Allowed from closed channel any number of times
      \item Sending to a closed channel panics
    \end{itemize}
  \item Range: \lstinline|for v := range ch { ... }| runs until channel is closed and drained
\end{enumerate}

Ownership Pattern:
\begin{enumerate}[\roman*.]
  \item Owner goroutine makes, writes, and closes channel
  \item Expose only \lstinline|<-chan T| to consumers
\end{enumerate}

\colbreak
\subsection{select}
\lstinline|select| composes channel operations:
\begin{enumerate}[\roman*.]
  \item Blocks if no case is ready (unless \lstinline|default|)
  \item If multiple cases are ready: runtime picks pseudo-randomly (uniform over ready cases)
  \item Stopping:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item \lstinline|time.After(d)|: channel that fires after \lstinline|d|
      \item \lstinline|done| channel to signal stop
    \end{itemize}
\end{enumerate}

For-select Loop: 
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
for {
  select {
  case <-done:
    return
  case <-time.After(1 * time.Second):
    // timed out
  default:
    // do work
  }
}
\end{lstlisting}

\subsection{sync Package}
Use mostly in small scopes (e.g., inside a \lstinline|struct|); prefer higher-level coordination with channels when possible.
\begin{enumerate}[\roman*.]
  \item WaitGroup: \lstinline|var wg sync.WaitGroup| 
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item \lstinline|wg.Add(n)|, \lstinline|wg.Done()|, \lstinline|wg.Wait()|, \lstinline|wg.Go(f)|
    \end{itemize}
  \item Mutex: \lstinline|var mu sync.Mutex|:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Locks: \lstinline|mu.Lock()|, \lstinline|mu.Unlock()|
    \end{itemize}
  \item Read-Write Mutex: \lstinline|var rw sync.Mutex|:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Reads: \lstinline|rw.RLock()|, \lstinline|rw.RUnlock()|
      \item Writes: \lstinline|rw.Lock()|, \lstinline|rw.Unlock()|
    \end{itemize}
  \item Cond: \lstinline|c := sync.NewCond(&mu)|
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Wait: \lstinline|c.Wait()| unlocks \& blocks, then re-locks before returning
      \item Wake: \lstinline|c.Signal()| / \lstinline|c.Broadcast()|
    \end{itemize}
  \item Once: \lstinline|once.Do(f)| runs \lstinline|f| at most once
  \item Pool: \lstinline|p.Get()| / \lstinline|p.Put(x)|
\end{enumerate}

\colbreak
\subsection{Memory Model}
Go's Memory Model: 
\begin{enumerate}[\roman*.]
  \item Sequenced-Before: goroutine program order
  \item Synchronized-Before:  
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item \lstinline|go f()| $\rightarrow$ goroutine \lstinline|f| begins
      \item Channel send $\rightarrow$ matching receive completes
      \item \lstinline|close(c)| $\rightarrow$ zero-value receive from closed \lstinline|c|
      \item Unbuffered receive $\rightarrow$ matching send completes
      \item Buffered $k^{th}$ receive $\rightarrow$ $(k+C)^{th}$ send completes
    \end{itemize}
  \item Happens-Before: transitive visibility relation of Sequenced-Before $\cup$ Synchronized-Before 
  \item Guarantee a read $r$ of variable $v$ observes write $w$ if:
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item $w$ happens before $r$
    \item Any other write to $v$ happens before $w$ or after $r$
  \end{itemize}
\end{enumerate}
\vspace{-.5em}
\subsection{Concurrency Patterns}
Patterns:
\begin{enumerate}[\roman*.]
  \item Confinement:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Ad-hoc: only one goroutine mutates shared data
      \item Lexical: restrict access to shared locations\\(e.g., expose only \lstinline|<-chan T|, slice of array)
    \end{itemize}
  \item Prevent goroutine leaks:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Creator ensures termination when work done, unrecoverable error, or when told to stop working 
      \item Beware nil channels: read/write blocks forever
    \end{itemize}
  \item Error handling:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Return result + error along the same channel
      \item Centralize decisions in a state-goroutine
    \end{itemize}
  \item Pipeline:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Stages connected by channels; each stage is a group of goroutines running the same function
    \end{itemize}
  \item Fan-out / Fan-in:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Fan-out: replicate a slow stage across goroutines 
      \item Fan-in: multiplex input chans into 1 output chan
    \end{itemize}
\item Load balancing:
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item Pool of workers reads from a shared work channel
    \item Each worker emits results; collect with fan-in
  \end{itemize}
\end{enumerate}
\vspace{-1em}
\colbreak
\subsubsection{Confinement}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
func owner() <-chan int {
  out := make(chan int) // owned by function

  go func() {
    defer close(out) // owner closes channel
    for i := 0; i < 5; i++ {
      out <- i // only owner goroutine sends
    }
  }()

  return out // expose receive-only channel
}
\end{lstlisting}

\subsubsection{For-Select Loop}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
func worker(done <-chan struct{}, in <-chan int) {
  for {
    select {
    case <-done:
      return
    case x, ok := <-in:
      if !ok { return }
      work(x)
    }
  }
}
\end{lstlisting}

\subsubsection{Prevent Goroutine Leak}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
func doWork(done <-chan struct{},
            in <-chan string) <-chan struct{} {
  terminated := make(chan struct{})

  go func() {
    defer close(terminated) // signal goroutine exited

    for {
      select {
      case s, ok := <-in: 
        if !ok { return } // no blocked goroutine left behind
        fmt.Println(s)
      case <-done:
        return // caller can force termination
      }
    }
  }()

  return terminated // caller can wait for cleanup
}
\end{lstlisting}
\colbreak

\subsubsection{Error Handling}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
type Result struct {
  Err  error
  Resp *http.Response
}

func check(done <-chan struct{},
           urls ...string) <-chan Result {
  out := make(chan Result)

  go func() {
    defer close(out)

    for _, u := range urls {
      resp, err := http.Get(u)
      r := Result{Err: err, Resp: resp}

      select {
      case <-done:
        return
      case out <- r: // send value/error together
      }
    }
  }()

  return out
}
\end{lstlisting}

\subsubsection{Pipeline}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
func gen(xs ...int) <-chan int {
  out := make(chan int)
  go func() {
    defer close(out)
    for _, x := range xs {
      out <- x
    }
  }()
  return out
}

func sq(in <-chan int) <-chan int {
  out := make(chan int)
  go func() {
    defer close(out)
    for x := range in {
      out <- x * x
    }
  }()
  return out
}
// stages compose: gen -> sq -> range
for x := range sq(gen(2, 3, 4)) {
  fmt.Println(x)
}
\end{lstlisting}

\subsubsection{Fan-Out / Fan-In}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
func fanIn(cs ...<-chan int) <-chan int {
  out := make(chan int)
  var wg sync.WaitGroup

  for _, c := range cs {
    wg.Add(1)
    go func(c <-chan int) {
      defer wg.Done()
      for x := range c { out <- x }
    }(c)
  }

  go func() { wg.Wait(); close(out) }()
  return out
}
// fan-out: many workers share randInts
for p := range fanIn(
  primeFinder(randInts),
  primeFinder(randInts),
  primeFinder(randInts),
) {
  fmt.Println(p)
}
\end{lstlisting}

\subsubsection{Load Balancing}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
type Request struct {
  fn    func() int
  reply chan int // per-request return path
}

func worker(reqs <-chan Request) {
  for r := range reqs { // workers pull load
    r.reply <- r.fn()
  }
}

func requester(reqs chan<- Request) {
  reply := make(chan int)

  for { // continuously generates load
    reqs <- Request{workFn, reply}
    result := <-reply // wait for own result
    process(result)
  }
}

reqs := make(chan Request) // shared work q
for i := 0; i < runtime.NumCPU(); i++ {
  go worker(reqs) // worker pool balances load
}

go requester(reqs)
\end{lstlisting}
\colbreak
\section{Classical Synchronisation Problems}
\lstset{style=c}
\subsection{Producer-Consumer}
Processes share a buffer, where producers add items and
consumers remove items when not empty.

Finite Buffer (init \lstinline|spaces = K, items = 0|):\\
\begin{minipage}{0.45\columnwidth}
  \begin{lstlisting}
// Producer
x = produce();

wait(spaces);
wait(mutex);
  buffer.add(x);
signal(mutex);
signal(items);
  \end{lstlisting}
\end{minipage}
\begin{minipage}{0.5\columnwidth}
  \begin{lstlisting}
// Consumer
wait(items);
wait(mutex);
  x = buffer.get();
signal(mutex);
signal(spaces);

consume(item);
  \end{lstlisting}
\end{minipage}
\vspace{-1em}
\subsection{Reader-Writer}
Processes share a critical region, where readers can read simultaneously but writers must have exclusive access.
\vspace{-1em}
\begin{minipage}{0.45\columnwidth}
  \begin{lstlisting}
// Writer
wait(empty);
write();
signal(empty);
  \end{lstlisting}
\end{minipage}
\begin{minipage}{0.5\columnwidth}
  \begin{lstlisting}
// Reader 
wait(mutex);
readers++;
if (readers == 1)
  wait(empty);
signal(mutex);

read();

wait(mutex);
readers--;
if (readers == 0)
  signal(empty);
signal(mutex);
  \end{lstlisting}
\end{minipage}
\begin{enumerate}[$-$]
  \item Writer Starvation: possible if readers keep entering and \lstinline|empty| is never signalled. Solutions:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item Queue: all pass through \lstinline|wait(queue)| 
      \item Writer-Priority: readers entering must \lstinline|wait(readTry)|; but first waiting writer blocks new readers with \lstinline|wait(readTry)| and does \lstinline|signal(readTry)| when done 
    \end{itemize}
\end{enumerate}
\vspace{-1em}
\colbreak
\subsection{Barrier}
C++:
\begin{lstlisting}[style=cpp]
std::barrier b(N, on_done);

auto work = [&](string name) {
  cout << name << " worked\n";
  b.arrive_and_wait();

  cout << name << " cleaned\n";
  b.arrive_and_wait();
};
\end{lstlisting}

Go:
\begin{lstlisting}[style=go]
type Barrier struct {
  wg  sync.WaitGroup
  wg2 sync.WaitGroup
}

func (b *Barrier) Init(n int) {
  b.wg.Add(n)
  b.wg2.Add(n)
}

func (b *Barrier) Wait() {
  b.wg.Done()
  b.wg.Wait()
  b.wg.Add(1)

  b.wg2.Done()
  b.wg2.Wait()
  b.wg2.Add(1)
} 
\end{lstlisting}

\colbreak

\subsection{Sleeping Barber}
Barber sleeps until a customer wakes him. If all waiting chairs are full, the customer leaves.

C++ (\lstinline|customer, barber, customerDone, barberDone = 0|):\\
\begin{minipage}{0.48\columnwidth}
\begin{lstlisting}[style=cpp,basicstyle=\ttfamily\scriptsize]
// Barber
while (true) {
  customer.acquire();
  barber.release();

  cutHair();

  custDone.acquire();
  barbDone.release();
}
\end{lstlisting}
\end{minipage}
\begin{minipage}{0.48\columnwidth}
\begin{lstlisting}[style=cpp,basicstyle=\ttfamily\scriptsize]
// Customer
{
  scoped_lock lk{mut};
  if (customers == MaxCustomers + 1) {
    balk();
    return;
  }
  customers++;
}

customer.release();
barber.acquire();

getHairCut();

custDone.release();
barbDone.acquire();

scoped_lock lock{mut};
customers--;
\end{lstlisting}
\end{minipage}

Go (\lstinline[style=go]|chairs : chan int [N], barber : chan struct {}|):\\
\begin{minipage}{0.48\columnwidth}
\begin{lstlisting}[style=go,basicstyle=\ttfamily\scriptsize]
// Barber
for {
  bs.barber <- struct{}{}
  <-bs.chairs
  cutHair()
}
\end{lstlisting}
\end{minipage}
\begin{minipage}{0.48\columnwidth}
\begin{lstlisting}[style=go,basicstyle=\ttfamily\scriptsize]
// Customer
select {
case bs.chairs <- 1:
  <-bs.barber
  getHairCut()
default:
  balk()
}
\end{lstlisting}
\end{minipage}

\colbreak
\subsection{Dining Philosophers}
$N$ philosophers around a circular table, with a single chopstick between. $2$ chopsticks are needed to eat.
\begin{minipage}{0.48\columnwidth}
  \begin{lstlisting}
// Philosopher
void p(int i) {
  while (1) {
    // think
    take(i);
    eat();
    put(i);
  }
}

void safe_to_eat (int i) {
  if (state[i] == HUNGRY && state[LEFT] != EATING && state[RIGHT] != EATING) {
    state[i] = EATING;
    signal(s[i]);
  }
}
  \end{lstlisting}
\end{minipage}
\begin{minipage}{0.45\columnwidth}
  \begin{lstlisting}
// Take Chopstick
void take(int i) {
  wait(mutex);
  state[i] = HUNGRY;
  safe_to_eat(i);
  signal(mutex);
  wait(s[i]);
}

// Put Chopstick
void put(int i) {
  wait(mutex);
  state[i] = THINKING;
  safe_to_eat(LEFT);
  safe_to_eat(RIGHT)
  signal(mutex);
}
  \end{lstlisting}
\end{minipage}
Limited Eater Solution, when at most $N-1$ philosophers eat concurrently, it's guaranteed at least one can eat:
\begin{lstlisting}
void philosopher(int i) {
  while (1) {
    // think 
    wait(seats);
    wait(chopstick[LEFT]);
    wait(chopstick[RIGHT]);
    eat();
    signal(chopstick[RIGHT]);
    signal(chopstick[LEFT]);
    signal(seats);
  }
}
\end{lstlisting}
\colbreak
C++:
\begin{lstlisting}[style=cpp,basicstyle=\ttfamily\scriptsize]
struct DiningTable {
  FairMutex chopstick[N];
  FairSemaphore footman{N - 1};

  FairMutex& left(size_t i) {
    return chopstick[i];
  }

  FairMutex& right(size_t i) {
    return chopstick[(i + 1) % N];
  }

  void eat(size_t i) {
    footman.acquire();
    {
      std::scoped_lock lk{left(i), right(i)};
      eat_cb(i);
    }
    footman.release();
  }
};
\end{lstlisting}

Go:
\begin{lstlisting}[style=go,basicstyle=\ttfamily\scriptsize]
type ChpStick struct{}

type DiningTable struct {
  n   int
  chs []chan ChpStick
}

func (t *DiningTable) Init(n int) {
  t.n = n
  for i := 0; i < n; i++ {
    ch := make(chan ChpStick, 1)
    ch <- ChpStick{}
    t.chs = append(t.chs, ch)
  }
}

// left, right defined same as C++
// odd-even order avoids circular wait
func (t *DiningTable) Eat(pid int) {
  first, second := t.left(pid), t.right(pid)
  if pid%2 == 1 {
    first, second = second, first
  }

  <-first
  <-second
  eat_cb(pid)
  second <- ChpStick{}
  first <- ChpStick{}
}
\end{lstlisting}
\vspace{-1em}
\colbreak
\subsubsection{H2O}

C++:
\begin{lstlisting}[style=cpp,basicstyle=\ttfamily\scriptsize]
struct WaterFactory {
  std::counting_semaphore<> oxygenSem{1};
  std::counting_semaphore<> hydrogenSem{2};
  std::barrier<> bar{3};

  void oxygen(void (*bond)()) {
    oxygenSem.acquire();    // at most 1 
    bar.arrive_and_wait();  // wait for HHO 
    bond();
    oxygenSem.release();
  }

  void hydrogen(void (*bond)()) {
    hydrogenSem.acquire();  // at most 2
    bar.arrive_and_wait();  // wait for HHO 
    bond();
    hydrogenSem.release();
  }
};
\end{lstlisting}

Go:
\begin{lstlisting}[style=go,basicstyle=\ttfamily\scriptsize]
type WaterFactory struct {
  oxygenMutex chan struct{}
  precomH     chan chan struct{}
}

// Init with oxygenMutex <- struct{}{}

func (wf *WaterFactory) Hydrogen(bond func()) {
  commit := make(chan struct{})

  wf.precomH <- commit // precommit
  <-commit             // commit
  bond()
  commit <- struct{}{} // postcommit
}

func (wf *WaterFactory) Oxygen(bond func()) {
  <-wf.oxygenMutex // become leader

  h1 := <-wf.precomH
  h2 := <-wf.precomH

  h1 <- struct{}{} // release 2 H atoms
  h2 <- struct{}{}
  bond()
  <-h1 // wait for 2 H atoms to finish
  <-h2

  wf.oxygenMutex <- struct{}{} // step down
}
\end{lstlisting}
\vspace{-1em}
\colbreak
\section{Rust}
\lstset{style=rust}

\subsection{Ownership Model}
Rust enforces memory-safety at compile-time without GC, preventing data races and enabling concurrency.
\begin{enumerate}[\roman*.]
  \item Each value has exactly one owner 
\end{enumerate}
\begin{enumerate}[$+$]\vspace{-1pt}
  \item Ownership prevents double-free; owner frees 
  \item Borrowing prevents use-after-free (UAF)
  \item Segfault-free
\end{enumerate}

Operations:
\begin{enumerate}[\roman*.]
  \item Move: \lstinline|T|
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item Transfers ownership; old binding cannot be used
    \item Deep Copy: \lstinline|clone()| is explicit (expensive)
  \end{itemize}
  \item Shared Borrow: \lstinline|&T|
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item Both bindings are read-only while shared
  \end{itemize}
  \item Mutable Borrow: \lstinline|&mut T|
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item Original binding cannot be used while borrowed
    \item Borrowed binding has exclusive mutable access
  \end{itemize}
\end{enumerate}

\subsection{Threads}

Operations (\lstinline|std::thread|):
\begin{enumerate}[\roman*.]
  \item Spawn: \lstinline|let h = thread::spawn(move| $\mid\mid$ \lstinline|{...})|
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item \lstinline|move|: moves captured values into thread
    \item Captured values must be \lstinline|Send + 'static|
  \end{itemize}
  \item Scoped Threads: \lstinline|thread::scope(|\dots\lstinline|)|
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item Threads finish before scope exits
    \item Can borrow local non-\lstinline|'static| data safely
  \end{itemize}
  \item Join: \lstinline|h.join()|
  \item Marker Traits:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item \lstinline|Copy|: safe to memcpy (i.e. \lstinline|u32, f32|, no String) 
    \item \lstinline|Send|: safe to transfer to another thread 
    \item \lstinline|Sync|: safe to share reference between threads;\\
      \lstinline|T| is \lstinline|Sync| $\iff$ \lstinline|&T| is \lstinline|Send|
  \end{itemize}
  \item Shared Ownership:
    \begin{itemize}[leftmargin=*]\vspace{3pt}
      \item \lstinline|Rc<T>|: within a thread
      \item \lstinline|Arc<T>|: across threads if \lstinline{T} is thread-safe
  \end{itemize}
\end{enumerate}
\vspace{-1em}
\colbreak
\subsubsection{Move vs Borrow}
\begin{lstlisting}[style=rust,basicstyle=\ttfamily\scriptsize]
fn take(v: Vec<i32>) { }  // takes ownership
fn read(v: &Vec<i32>) { } // shared borrow

let v = vec![1, 2, 3];
read(&v);    // v still usable
take(v);     // v moved
// read(&v); // compile error
\end{lstlisting}
\vspace{-.5em}
\subsubsection{Spawn + Join}
\begin{lstlisting}[style=rust,basicstyle=\ttfamily\scriptsize]
let v = vec![1, 2, 3];

// move transfers v into the spawned thread
let h = thread::spawn(move || {
  println!("{:?}", v);
  42 // thread return value
});

// println!("{:?}", v); // compile error: moved
let result = h.join().unwrap();
println!("{}", result);
\end{lstlisting}
\vspace{-.5em}
\subsubsection{Shared State Across Threads}
\begin{lstlisting}[style=rust,basicstyle=\ttfamily\scriptsize]
let c = Arc::new(Mutex::new(0));
let mut hs = vec![];

for _ in 0..4 {
  let c = Arc::clone(&c);
  hs.push(thread::spawn(move || {
    *c.lock().unwrap() += 1;
  }));
}

for h in hs { h.join().unwrap(); }

println!("{}", *c.lock().unwrap());
\end{lstlisting}

\subsubsection{Scoped Threads}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
let mut xs = vec![1, 2, 3, 4];

thread::scope(|s| {
  let (a, b) = xs.split_at_mut(2);

  s.spawn(|| for x in a { *x += 1 });
  s.spawn(|| for x in b { *x += 1 });
}); // all scoped threads joined here

println!("{:?}", xs);
\end{lstlisting}
\vspace{-1em}
\colbreak
\subsection{Mutexes}

Operations (\lstinline|std::sync::Mutex|):
\begin{enumerate}[\roman*.]
  \item Construct: \lstinline|let m = Mutex::new(x)|
  \item Lock-Guard: \lstinline|let mut g = m.lock().unwrap()|
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item Guard controls access: \lstinline|*g += 1|
    \item When guard goes out of scope, mutex unlocks
  \end{itemize}
  \item Poisoning:
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item Panic while holding lock makes later \lstinline|lock()| return \lstinline|Err|
    \item \lstinline|m.lock().unwrap()| propagates poisoning as panic
  \end{itemize}
  \item Interior Mutability:
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item \lstinline|Mutex<T>| allows mutation through shared ownership with synchronization
  \end{itemize}
\end{enumerate}

\subsection{Atomics}
Atomics provide indivisible shared-memory operations.

Operations (\lstinline|std::sync::atomic|):
\begin{enumerate}[\roman*.]
  \item Construct: \lstinline|let a = AtomicUsize::new(v)|
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item \lstinline|AtomicBool|, \lstinline|AtomicIsize|, \lstinline|AtomicI16|
  \end{itemize}
  \item Load: \lstinline|a.load(SeqCst)|
  \item Store: \lstinline|a.store(v, SeqCst)|
  \item Read-Modify-Write:\\\lstinline|a.fetch_add(1, SeqCst)|, \lstinline|a.swap(v, SeqCst)|
\end{enumerate}

\subsection{Multi-Producer, Single Consumer (MPSC)}
MPSC provides a FIFO communication queue.

Operations (\lstinline|std::sync::mpsc|):
\begin{enumerate}[\roman*.]
  \item Create: \lstinline|let (tx, rx) = channel()|
  \item Clone Sender: \lstinline|let tx2 = tx.clone()|
  \item Send: \lstinline|tx.send(v)|
  \item Receive: \lstinline|rx.recv()|
\end{enumerate}

\colbreak
\subsection{crossbeam Package}
Crossbeam extends beyond \lstinline|std|.

Features:
\begin{enumerate}[\roman*.]
  \item Bounded Channels: \lstinline|crossbeam::bounded(C)|
  \item Backoff: \lstinline|crossbeam_utils::Backoff::new()|
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item Exponentially slow retry reduces contention
    \item Busywait: \lstinline|.spin()| / Yield: \lstinline|.snooze()|
  \end{itemize}
\end{enumerate}

\subsection{rayon Package}
Rayon provides data-parallelism operations.

Features (\lstinline|rayon::prelude::*|):
\begin{enumerate}[\roman*.]
  \item Iterators: \lstinline|xs.par_iter()|, \lstinline|xs.par_iter_mut()|
  \item Combinators:
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item \lstinline|map(...)|, \lstinline|filter(...)|, \lstinline|for_each(...)|
    \item Aggregation: \lstinline|reduce(...)|/ \lstinline|sum()| 
  \end{itemize}
  \item Sort: \lstinline|xs.par_sort()|/ \lstinline|xs.par_sort_unstable()|
\end{enumerate}

\subsubsection{Parallel Max}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
let max = AtomicI64::new(MIN);
vec.par_iter().for_each(|n| {
  loop {
    let old = max.load(SeqCst);
    if *n <= old { break; }
    let r = max.compare_and_swap(old, *n, SeqCst);
    if r == old {
      // swapped 
      break;
    }
  }
})
\end{lstlisting}

\colbreak
\subsection{Non-blocking I/O}
Non-blocking I/O enables concurrency with one thread:
\begin{enumerate}[\roman*.]
  \item Blocking I/O: thread sleeps until data arrives
  \item \lstinline|epoll|: kernel reports ready file descriptors
  \item Event Loop: wait for ready fds $\rightarrow$ handle I/O
\end{enumerate}

\subsection{Futures}
Futures represent work that will produce a value later: 
\begin{enumerate}[\roman*.]
  \item \lstinline|Future|: trait driven by \lstinline|poll(Context)|:
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item Runs until it can no longer make progress
    \item Returns \lstinline|Poll::Ready(T)| or \lstinline|Poll::Pending|
  \end{itemize}
  \item \lstinline|Context| provides \lstinline|wake()| (notify executor to repoll)
  \item Combinators: \lstinline|.then()|
\end{enumerate}

Executors are user-space schedulers that drive futures:
\begin{enumerate}[\roman*.]
  \item \lstinline|Pending| $\Rightarrow$ future arranges a \lstinline|wake()| when it can progress again
  \item If nothing can progress: sleep until a \lstinline|wake()|
\end{enumerate}

\subsection{Async/Await}
Async/Await are syntactic sugar for composing Futures.
\begin{enumerate}[\roman*.]
  \item \lstinline|async fn| returns a \lstinline|Future|
  \item \lstinline|.await| waits for a future (only inside \lstinline|async|)
\end{enumerate}
\begin{enumerate}[$+$]\vspace{-1pt}
  \item Zero-cost Abstraction: compiler lowers \lstinline|async fn| into a state machine implementing \lstinline|poll()|
  \item Lower memory use with stackless coroutines
  \item Lower scheduling overhead from context switches
  \item Scales to very high concurrency (esp. I/O bound)
\end{enumerate}
\begin{enumerate}[$-$]\vspace{-1pt}
  \item Block inside async sleeps the executor thread
  \item Cooperative: tasks yield only at \lstinline|.await|
  \item CPU-heavy work can starve other tasks 
  \item No recursion; future size fixed at compile time
\end{enumerate}
\vspace{-1em}
\colbreak

\subsection{Tokio}
Tokio is an async runtime: executor + reactor + async I/O.

Features:
\begin{enumerate}[\roman*.]
  \item Runtime macro: \lstinline|#[tokio::main]|
  \item Spawn: \lstinline|tokio::spawn(async move {...})|
  \item Join: \lstinline|handle.await|
  \item Blocking work: \lstinline|tokio::task::spawn_blocking(...)|
  \item Async sync: \lstinline|tokio::sync::{Mutex, Semaphore, Barrier}|
\end{enumerate}

\subsubsection{Tokio Tasks}
\begin{lstlisting}[basicstyle=\ttfamily\scriptsize]
#[tokio::main]
async fn main() {
  let mut hs = vec![];

  for i in 0..4 {
    hs.push(tokio::spawn(async move {
      work(i).await
    }));
  }

  for h in hs {
    println!("{:?}", h.await.unwrap());
  }
}
\end{lstlisting}
\colbreak
\section{Testing}
\lstset{style=c}
Bug Types:
\begin{enumerate}[\roman*.]
\item Unwanted Blocking:
  \begin{itemize}[leftmargin=*]
    \item Deadlock/Livelock: no progress, operations are not "completed", effects do not become visible
    \item IO: waiting for external input
  \end{itemize}
\item Race Conditions:
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item Data races $\Rightarrow$ undefined behavior (unsynchronized concurrent access)
    \item Broken invariants:
      \begin{itemize}[leftmargin=*]\vspace{5pt}
        \item Double-free 
        \item Dangling pointers $\implies$ UAF
        \item Memory corruption from read partial update
      \end{itemize}
    \item Lifetime issues: thread outlives data it accesses
  \end{itemize}
\end{enumerate}

Techniques:
\begin{enumerate}[\roman*.]
\item Review the code:
\begin{itemize}[leftmargin=*]\vspace{3pt}
\item Identify shared data and how it is protected
\item Track held mutexes and find lock ordering issues
\item Check cross-thread ordering enforcement
\item Validate lifetimes (e.g. \lstinline|join()| on exceptions)
\end{itemize}
\item Testing:
\begin{itemize}[leftmargin=*]\vspace{3pt}
\item Run the smallest amount of code that can demonstrate the problem
\item Remove concurrency to confirm it is concurrency-related
\item Run on multicore and (when useful) single-core
\end{itemize}
\end{enumerate}
\colbreak
\subsection{Dynamic Instrumentation}

Valgrind:
\begin{enumerate}[\roman*.]
  \item Shadows registers and memory to validate memory operations:\\each allocation freed once, each access to allocated space, each reach from initialized memory
  \item High overhead: \lstinline|10-20x|
  \item Usage: \lstinline|valgrind --tool=memcheck <prog ...>|
\end{enumerate}

Helgrind:
\begin{enumerate}[\roman*.]
  \item Intercepts \lstinline|pthread| operations to validate thread operations:\\miuse of POSIX threads API, potential deadlocks from lock-order cycles, data races from happens-before analysis
  \item Very high overhead: \lstinline|~100x|
  \item Usage: \lstinline|valgrind --tool=helgrind <prog ...>|
\end{enumerate}

\subsection{Compiler Instrumentation}

Sanitizers:
\begin{enumerate}[\roman*.]
  \item Compile-time instrumentation; supported by \lstinline|clang| and \lstinline|gcc|
  \item Lower overhead: \lstinline|~5-10x|
  \item Instruments memory accesses / function boundaries and tracks memory state
\end{enumerate}

AddressSanitizer (ASan):
\begin{enumerate}[\roman*.]
  \item Detects addressability issues
  \item Typical cost: \lstinline|~2x| slowdown, \lstinline|1.5-3x| memory overhead
  \item Usage: \lstinline|-fsanitize=address|
\end{enumerate}

ThreadSanitizer (TSan):
\begin{enumerate}[\roman*.]
  \item Detects data races using happens-before tracking
  \item Also reports thread / mutex / lock-order issues
  \item Typical cost: \lstinline|5-15x| slowdown, \lstinline|5-10x| memory overhead
  \item Usage: \lstinline|-fsanitize=thread -fPIE -pie|
\end{enumerate}

\colbreak
\subsection{Model Checking}

Check Formal Specifications:
\begin{enumerate}[\roman*.]
  \item Build a model in a special DSL / new language
  \item Check: constraints, unexpected behaviour, deadlock
  \item Why spend time:
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item Check designs before costly implementation
    \item Prove properties for existing code
    \item Enable aggressive optimizations without breaking correctness
  \end{itemize}
\end{enumerate}
\begin{enumerate}[$+$]\vspace{-1pt}
  \item Rigorous, check all traces exhaustively 
  \item Produces trace violating requirement
\end{enumerate}
\begin{enumerate}[$-$]\vspace{-1pt}
  \item Tedious to define correct specification
\end{enumerate}

Approaches:
\begin{enumerate}[\roman*.]
  \item Write a formal specification and check it\\(with model checker/ proof assistant)
  \item Use specification to write minimal code:
  \item Add invariants as comments to assist checker:
\end{enumerate}

Model Checkers:
\begin{enumerate}[\roman*.]
  \item TLA+ (TLC): temporal properties; good for concurrent/distributed systems
  \begin{itemize}[leftmargin=*]\vspace{3pt}
    \item Write a TLA+ specification; use TLC to exhaustively explore the state space
    \item Check safety (invariants) and liveness (temporal)
    \item Reports errors with a counterexample trace 
    \item Model Structure:\\\lstinline|Init| (initial states) + \lstinline|Next| (state transitions)
    \item Uses fairness to rule out infinite ``stuttering'' (non-progress) executions
    \end{itemize}
  \item Coq: interactive proofs; generate code (OCaml/Haskell/Scheme)
  \item Alloy: relational logic; good for modeling structures
\end{enumerate}

Challenges of Distributed Systems:
\begin{enumerate}[\roman*.]
  \item Reliability: in case of failures 
  \item No global clock or ordering: need memory model 
  \item Consistency 
  \item Consensus
\end{enumerate}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                       End                          %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\end{multicols*}
\end{document}
